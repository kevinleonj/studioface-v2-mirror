# Verified vendor facts (append-only; date | fact | source)
- 2026-09-16 | fal nano-banana-2/edit: image_urls (<=14), resolution 0.5K/1K/2K/4K, output_format jpeg/png/webp, $0.08 per 1K image | https://fal.ai/models/fal-ai/nano-banana-2/edit/api
- 2026-09-16 | fal unsupported_image_format is HTTP 422, not retryable | Context7 /websites/fal_ai (fal.ai/docs/model-apis/errors)
- 2026-09-16 | Cloud Run request-based billing throttles CPU after response; no background threads | https://docs.cloud.google.com/run/docs/tips
- 2026-09-16 | Cloud Run v2 env value_source.secret_key_ref {secret, version} | Context7 /hashicorp/terraform-provider-google
- 2026-09-16 | Stripe Terraform provider lukasaron/stripe ~>3.4: stripe_product, stripe_price, stripe_webhook_endpoint(.secret) | Context7 /lukasaron/terraform-provider-stripe
- 2026-09-16 | Next.js static export: output: 'export' in next.config, server features unsupported | Context7 /vercel/next.js
- 2026-09-16 | Claude Code -p defaults to permission mode default; pass --permission-mode auto; auto blocks deploys, secret writes, DNS, IAM, unapproved PR merge; pushing to main of the working repo is allowed | https://code.claude.com/docs/en/permission-modes
- 2026-09-16 | Hooks: PreToolUse exit 2 blocks; PostToolUse exit 2 shows stderr; Stop exit 2 prevents stopping; TaskCompleted exit 2 blocks completion; stop_hook_active anti-loop field | https://code.claude.com/docs/en/hooks
- 2026-09-16 | Turnstile: free, unlimited siteverify; Terraform cloudflare_turnstile_widget validated against provider 5.25 | https://blog.cloudflare.com/turnstile-ga/ ; terraform validate
- 2026-09-16 | -p never shows the trust dialog; settings-file hooks ARE used in -p in an untrusted folder; permissions.allow rules are NOT until trusted; manual trust = projects["<root>"].hasTrustDialogAccepted=true in ~/.claude.json; .mcp.json servers connect without asking in -p | https://code.claude.com/docs/en/permissions
- 2026-09-16 | CLI: claude auth status (exit 0/1), --permission-mode with -p, --permission-prompts none (v2.1.259+), --effort, --max-turns, --max-budget-usd, --output-format stream-json --verbose, --name | https://code.claude.com/docs/en/cli-reference
- 2026-09-16 | gcloud billing accounts list --filter=open=true | https://docs.cloud.google.com/sdk/gcloud/reference/beta/billing/accounts/list
- 2026-09-16 | Kevin's user-level guard_bash allows git push only when .claude/state/ci_mirror_ok is fresh; make ci now touches it | technical-reviewer mistakes-catalog D4
- 2026-09-17 | Stripe official remote MCP https://mcp.stripe.com, OAuth, `claude mcp add --transport http stripe https://mcp.stripe.com/` | https://docs.stripe.com/mcp
- 2026-09-17 | Cloudflare official remote MCP https://mcp.cloudflare.com/mcp, OAuth, plus per-service servers | https://developers.cloudflare.com/agents/model-context-protocol/cloudflare/servers-for-cloudflare/
- 2026-09-17 | winget id Hashicorp.Terraform installs 1.16.2 on Kevin's box (his output) | terminal
- 2026-09-17 | google-beta google_firebase_project + google_firebase_hosting_site; terraform validate green with google-beta 7.46.1 | Context7 /hashicorp/terraform-provider-google + terraform validate
- 2026-09-17 | keyring on Windows uses WinVaultKeyring (Credential Manager), needs pywin32/pywin32-ctypes, priority 5 | Context7 /jaraco/keyring
- 2026-09-17 | google provider reads GOOGLE_OAUTH_ACCESS_TOKEN when no credentials set; 1-hour, non-renewable | Context7 /hashicorp/terraform-provider-google (provider.go, provider_reference)
- 2026-09-17 | gcs backend accepts access_token / GOOGLE_OAUTH_ACCESS_TOKEN | https://developer.hashicorp.com/terraform/language/backend/gcs
- 2026-09-17 | keyring: built-in Windows Credential Manager backend; set_password/get_password/delete_password | Context7 /jaraco/keyring
- 2026-09-17 | google provider: gcloud user credentials belong to a Google-owned project; set user_project_override=true (+ billing_project) for quota-project APIs | Context7 /hashicorp/terraform-provider-google provider_reference
- 2026-09-17 | cloudflare_dns_record import id '<zone_id>/<dns_record_id>'; token needs DNS Read + DNS Write | Context7 /cloudflare/terraform-provider-cloudflare
- 2026-09-17 | WIF setup requires iam.googleapis.com enabled (plus sts, iamcredentials) | multiple 2026 guides; Google WIF docs
- 2026-09-17 | siteverification.googleapis.com needs its own OAuth scope; gcloud user access tokens (cloud-platform) get ACCESS_TOKEN_SCOPE_INSUFFICIENT; domain verification stays manual per domain | Kevin's terraform output
- 2026-09-17 | Managed Cloud Run domain mappings: `gcloud beta run domain-mappings list --region R`, `describe --domain D --region R` (component beta) | cloud.google.com/sdk/gcloud/reference/beta/run/domain-mappings
- 2026-09-17 | fal_client.subscribe(application_id, arguments, *, with_logs, on_queue_update) -> dict; result["images"][0]["url"]; signature confirmed by inspect on the installed fal-client 1.0.1 | Context7 /websites/fal_ai + https://fal.ai/docs/api-reference/client-libraries/python/index
- 2026-09-17 | fal_client.subscribe_async(...) exists and is awaited; no separate AsyncClient class is documented, so asyncio.gather over subscribe_async is the supported concurrency path | https://fal.ai/docs/documentation/model-apis/inference/synchronous
- 2026-09-17 | fal_client.upload(data: bytes, content_type: str) -> str CDN URL; upload_async is the awaitable twin; both confirmed present on installed 1.0.1 | https://fal.ai/docs/documentation/model-apis/fal-cdn
- 2026-09-17 | fal-ai/nano-banana-2/edit input: prompt (required), image_urls, resolution enum "0.5K"/"1K"/"2K"/"4K" default "1K", output_format enum jpeg/png/webp DEFAULT png (so jpeg must be passed explicitly), aspect_ratio enum includes "4:5" default "auto" | https://fal.ai/models/fal-ai/nano-banana-2/edit/api
- 2026-09-17 | fal exceptions: FalClientError base; FalClientHTTPError(message, status_code, response_headers, response, error_type); FalClientTimeoutError(timeout, request_id) | https://github.com/fal-ai/fal/blob/main/projects/fal_client/src/fal_client/client.py
- 2026-09-17 | fal queue requests auto-retry on 503/504/429/timeouts up to 10 times with backoff; failed 5xx are not billed | https://fal.ai/docs/model-apis/model-endpoints/reliability
- 2026-09-17 | CORRECTION to the 2026-09-16 line above: the "image_urls <= 14" cap is NOT documented anywhere on the nano-banana-2/edit schema page; do not rely on it (we send at most 4) | Context7 /websites/fal_ai re-check
- 2026-09-17 | pillow-heif 1.7.0 on this machine both decodes AND encodes HEIF (Image.save(buf,"HEIF") produces ftypheic), so HEIC tests use a real container, not a stub header | runtime: .venv python round-trip
- 2026-09-17 | Blob.generate_signed_url(version="v4", expiration=timedelta, method="GET", service_account_email=..., access_token=...) — all five params confirmed present on installed google-cloud-storage 3.14.1 by inspect; v4 max expiry 604800s (7 days) | https://docs.cloud.google.com/storage/docs/access-control/signed-urls + runtime inspect
- 2026-09-17 | On Cloud Run the ambient credentials hold a token and no private key, so generate_signed_url hits _signing.ensure_signed_credentials and raises AttributeError "you need a private key to sign credentials"; supplying BOTH service_account_email and access_token switches it to the IAM signBlob path | https://raw.githubusercontent.com/googleapis/python-storage/main/google/cloud/storage/_signing.py
- 2026-09-17 | signBlob needs permission iam.serviceAccounts.signBlob on the target service account (roles/iam.serviceAccountTokenCreator) | https://docs.cloud.google.com/iam/docs/reference/credentials/rest/v1/projects.serviceAccounts/signBlob
- 2026-09-17 | compute_engine.Credentials.service_account_email is "not guaranteed to be set until refresh() has been called" — refresh before reading it or the email is the literal string "default" | https://github.com/googleapis/google-auth-library-python/blob/main/docs/reference/google.auth.compute_engine.credentials.md
- 2026-09-17 | Blob.from_uri(uri, client) is the non-deprecated gs:// parser (from_string is a deprecated alias); both present on 3.14.1 | https://github.com/googleapis/python-storage/blob/main/docs/storage/blob.md + runtime inspect
- 2026-09-17 | UNVERIFIED, needs a live check: whether the Cloud Run runtime SA needs roles/iam.serviceAccountTokenCreator bound ON ITSELF to signBlob for itself. Self-IMPERSONATION (generateAccessToken) is rejected with FAILED_PRECONDITION, but signBlob is a different API and the researcher could not find an authoritative statement either way | open item, see MORNING-REPORT.md
- 2026-09-17 | GA4 MP: POST https://www.google-analytics.com/mp/collect with measurement_id + api_secret in the QUERY STRING; client_id and events in the body; debug twin is /debug/mp/collect | https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference
- 2026-09-17 | GA4 MP purchase params required: currency (ISO 4217), value (number), transaction_id (string), items (array, each needs item_id or item_name); max 25 events/request, body < 130 kB | https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference/events
- 2026-09-17 | GA4 MP returns 2xx whenever the request is RECEIVED and never reports a malformed payload; only /debug/mp/collect returns validationMessages. A wrong payload is therefore undetectable at runtime — the unit tests are the only guard | https://developers.google.com/analytics/devguides/collection/protocol/ga4/validating-events
- 2026-09-17 | GA4 MP for a Web stream requires client_id in the body; the docs give no guidance for server-only events with no browser-generated id (open question) | https://developers.google.com/analytics/devguides/collection/protocol/ga4/sending-events
- 2026-09-17 | Google Front End answers the LITERAL path /healthz itself on every *.run.app host, before the request reaches the Cloud Run container. Measured on studioface-api revision 00004: /healthz -> 404, 1568 bytes, Google's own error page, no x-cloud-trace-context; /healthzz and /nonexistent-page -> 404, 12165 bytes, our Next.js 404 (container answered); /openapi.json -> 200 and listed /healthz. Sibling paths (/health, /livez, /readyz) pass through untouched, so a health check must not live on /healthz | live curl against the deployed revision + https://github.com/coldworkshq/doug/issues/300 and https://github.com/ramsesoriginal/lorenzo/pull/65
- 2026-09-17 | GA4 MP /debug/mp/collect validates payload STRUCTURE even with a placeholder measurement_id/api_secret: our purchase payload returned "validationMessages": [], and a deliberately broken control (event name "purchase!!") returned NAME_INVALID, so the empty result is meaningful. It does NOT prove the event reaches a specific property — that needs the real credentials | live curl, both directions
- 2026-09-17 | roles/iam.serviceAccountTokenCreator granted at PROJECT level to the Cloud Run runtime SA covers every service account in the project, itself included, so signBlob-for-self needs no separate self-binding. sa-studioface-api holds it project-wide (gcloud projects get-iam-policy); its own resource-level policy is empty | gcloud projects get-iam-policy + gcloud iam service-accounts get-iam-policy
- 2026-09-17 | FirestoreCounter.increment_if_below works against real Firestore in production: POST /api/recuperar from one IP returned 200 x5 then 429 x2, the RECOVER_PER_HOUR=5 ceiling, enforced across Cloud Run instances. Atomicity under genuinely concurrent writes is still unproven (one client, sequential) | live curl against api.studioface.app
- 2026-09-17 | Stripe success_url: `{CHECKOUT_SESSION_ID}` is a literal template string Stripe substitutes after payment, and the documented example puts it in the URL's own query string (`...?session_id={CHECKOUT_SESSION_ID}`), which is exactly the /api/gracias shape | https://docs.stripe.com/payments/checkout/custom-success-page?payment-ui=stripe-hosted
- 2026-09-17 | NOT CONFIRMED either way: that `{CHECKOUT_SESSION_ID}` is the ONLY success_url placeholder (inferred from the absence of any other), and whether success_url must be https or publicly reachable — the API reference says neither | https://docs.stripe.com/api/checkout/sessions/create
- 2026-09-17 | Stripe test mode and live mode are completely separate object spaces ("objects created in one mode are completely inaccessible to the other"; mode is decided strictly by the API key, not by the dashboard toggle), so a test-mode price id cannot be used with a live key | https://docs.stripe.com/testing-use-cases + https://docs.stripe.com/keys#test-live-modes
- 2026-09-17 | Stripe key prefixes: sk_test_/pk_test_/rk_test_ vs sk_live_/pk_live_/rk_live_ (also sk_org_). There is no CLI command documented to report the mode of the active key; the read-only check is GET /v1/balance, whose Balance object carries `livemode` | https://docs.stripe.com/keys + https://docs.stripe.com/api/balance/balance_retrieve
- 2026-09-17 | Stripe test card 4242 4242 4242 4242 takes any valid FUTURE expiry, any 3-digit CVC and arbitrary values elsewhere; test cards only function against test keys | https://docs.stripe.com/testing?testing-method=payment-methods
- 2026-09-17 | Stripe event replay: dashboard "Resend" works up to 15 days after the event; CLI `stripe events resend <event_id> --webhook-endpoint=<endpoint_id>` up to 30 days. A manual resend does NOT cancel Stripe's automatic retry behaviour even if it gets a 2xx | https://docs.stripe.com/webhooks + https://docs.stripe.com/cli/events/resend
- 2026-09-17 | NOT VERBATIM CONFIRMED: that a resend reuses the same `event.id`. Stripe's own best-practice text tells you to dedupe by logging processed event IDs, which is the pattern OrderStore.claim_event implements, so keying idempotency on event.id is the documented approach whatever resend does | https://docs.stripe.com/webhooks (Best practices, "Handle duplicate events")
- 2026-09-17 | NOT GUARANTEED by the object reference: that `customer_details.email` is always present on a completed payment-mode session. The docs say a valid customer email is REQUIRED to complete a Checkout Session (collected by Checkout, or prefilled via customer_email/customer/customer_account), but `customer_details` is documented as nullable. _order_from_session indexes it directly | https://docs.stripe.com/api/checkout/sessions/object
- 2026-09-17 | GitHub Actions ubuntu-latest (24.04) ships gcloud CLI and five JDKs (8, 11, 17 default, 21, 25), exposed as JAVA_HOME_<major>_X64. The Firestore emulator requires Java 21+, so the runner's default JAVA_HOME of 17 is NOT enough and the job must export JAVA_HOME_21_X64 | https://github.com/actions/runner-images/blob/main/images/ubuntu/Ubuntu2404-Readme.md + https://docs.cloud.google.com/firestore/docs/emulator
- 2026-09-17 | The emulator is a separate gcloud component id `cloud-firestore-emulator` (confirmed by `gcloud components list` on this box); installed in CI via google-github-actions/setup-gcloud with install_components, because the apt-installed gcloud on the runner has its component manager disabled | gcloud components list (runtime) + https://docs.cloud.google.com/firestore/docs/emulator
- 2026-09-17 | `gcloud emulators firestore start --host-port=HOST:PORT`; setting FIRESTORE_EMULATOR_HOST makes the client libraries connect to it and, per Google, "neither project ID nor credentials are required". There is no `gcloud emulators firestore env-init` (that exists only for the datastore emulator) | https://docs.cloud.google.com/sdk/gcloud/reference/emulators/firestore/start + https://docs.cloud.google.com/firestore/docs/emulator
- 2026-09-17 | LIMIT ON WHAT THE EMULATOR PROVES: it "uses simple locking" for transactions and does not reproduce production's concurrency control; a lock from a failed transaction can take up to 30 s to release; it also does not enforce composite indexes or production quotas. So an atomicity pass there proves our transaction never loses an update, not that production's retry semantics are identical | https://docs.cloud.google.com/firestore/docs/emulator
- 2026-09-17 | NOT DOCUMENTED: a verbatim readiness line or health endpoint for the Firestore emulator. CI therefore probes the TCP port rather than grepping stdout | https://docs.cloud.google.com/firestore/docs/emulator
- 2026-09-17 | Bizum: Spain only for the customer (Spanish IBAN), EUR only, one-time payments only (no recurring, no manual capture), min 0,50 EUR, max 5.000 EUR, full and partial refunds up to 395 days (asynchronous, confirmed by refund.updated/refund.failed), disputes supported. Supported in Checkout `mode: payment`; NOT in subscription or setup mode | https://docs.stripe.com/payments/bizum
- 2026-09-17 | Bizum enablement: Stripe's documented guidance is the Dashboard toggle plus dynamic payment methods, and explicitly "Don't pass `payment_method_types` when creating Checkout Sessions". Omitting payment_method_types IS what activates dynamic behaviour for Checkout (`automatic_payment_methods` is a PaymentIntents concept). Passing `payment_method_types: ['card','bizum']` is the documented manual alternative | https://docs.stripe.com/payments/bizum/accept-a-payment
- 2026-09-17 | Bizum onboarding beyond account activation: a business tax ID (company.tax_id/vat_id) or, for a Spanish sole trader, DNI/NIE via individual.id_number, plus business_type; the capability stays `pending` until verified | https://docs.stripe.com/payments/bizum
- 2026-09-17 | NOT CONFIRMED: any success_url / redirect handling unique to Bizum. The Checkout guide shows the ordinary success_url and nothing Bizum-specific; confirmation happens in the customer's banking app | https://docs.stripe.com/payments/bizum/accept-a-payment
- 2026-09-17 | INFERRED, not quoted: that the Dashboard payment-method toggle is set independently per mode. Stripe documents separate dashboard URLs (/test/settings/payment_methods vs /settings/payment_methods) and full object isolation between modes, but no sentence states the toggle itself is per-mode | https://docs.stripe.com/payments/payment-methods/dynamic-payment-methods + https://docs.stripe.com/keys
- 2026-09-17 | lukasaron/stripe provider: authenticates only via `api_key` / STRIPE_API_KEY. There is NO provider-level test/live switch — swapping the key is the only mechanism. `livemode` is exposed on stripe_tax_rate but NOT on stripe_product, stripe_price or stripe_webhook_endpoint | https://github.com/lukasaron/terraform-provider-stripe/blob/main/docs/index.md + docs/resources/*.md
- 2026-09-17 | Reversible rollback without deletion: `active = false` archives a stripe_price ("whether the price can be used for new purchases"); `disabled = true` disables a stripe_webhook_endpoint. Both exposed by the Terraform provider as well as the API | https://docs.stripe.com/api/prices/object + https://docs.stripe.com/api/webhook_endpoints/update
- 2026-09-17 | Stripe go-live checklist states outright that objects created in test mode (plans, coupons, products, SKUs) cannot be used in live mode. NO documented guidance exists for what a Terraform state holding test-mode ids does against a live key; the 404-on-refresh consequence is inference, which is why GO-LIVE.md uses a separate state prefix rather than a key swap | https://docs.stripe.com/get-started/checklist/go-live
- 2026-09-17 | Live mode requires account activation first (dashboard.stripe.com/account/onboarding: business, product and relationship information, KYC) | https://docs.stripe.com/get-started/account/activate

- 2026-09-17 | fal gpt-image-2 application id is `openai/gpt-image-2` (confirmed in the model page's own code snippet, `fal.subscribe("openai/gpt-image-2", ...)`). `fal-ai/gpt-image-2` ALSO resolves to the same schema, and `openai/gpt-image-1.5` 404s, so the router does validate ids. Context7 /websites/fal_ai has NO gpt-image-2 page and fal.ai/docs/.../gpt-image-2 returns 404, so this was settled against the live API, not the docs | https://fal.ai/models/openai/gpt-image-2/api + live OpenAPI (below)
- 2026-09-17 | AUTHORITATIVE schema source for any fal endpoint: GET https://fal.ai/api/openapi/queue/openapi.json?endpoint_id=<id> with the fal key. This is generated from the deployed app, so it beats both Context7 and the marketing page | live call
- 2026-09-17 | openai/gpt-image-2 INPUT: prompt (string, REQUIRED, the only required field); num_images (int, default 1, minimum 1, MAXIMUM 4); image_size (preset enum square_hd|square|portrait_4_3|portrait_16_9|landscape_4_3|landscape_16_9|auto, or explicit {width,height}; default landscape_4_3; concrete sizes need both dims multiples of 16, max edge 3840, aspect <= 3:1, total pixels 655360..8294400); quality (auto|low|medium|high, default high); output_format (jpeg|png|webp, default png); background (auto|transparent|opaque, default auto); sync_mode (bool, default false) | live OpenAPI
- 2026-09-17 | openai/gpt-image-2 OUTPUT: {"images": [...]} of ImageFile; reach URLs at result["images"][i]["url"]. No revised-prompt or content-filter field in the schema | live OpenAPI
- 2026-09-17 | fal price for gpt-image-2 is NOT published on the model page or via any pricing API I could reach (fal.ai/api/pricing 404, api.fal.ai/v1/models/pricing 400). Spend is therefore MEASURED, not estimated: GET https://rest.alpha.fal.ai/billing/user_balance returns the account balance as a bare number (10.0 USD at 2026-09-17), so a script can read it before and after and report the real cost | live call
- 2026-09-17 | CAUTION measured the hard way: POST https://queue.fal.run/<id> with body {} returns 200 and QUEUES the request; it does not validate the body up front. Probing an endpoint's existence with an empty body is therefore not free-by-construction. Balance was unchanged at exactly 10.0 afterwards, so these particular probes did not bill, but use the OpenAPI route to check an endpoint instead | live call
- 2026-09-17 | fal content policy: a prompt that trips moderation returns HTTP 422 with type `content_policy_violation`; for an OpenAI-backed model OpenAI's own filter can reject through the same shape. fal does not document a specific rule on photorealistic faces of imaginary people | https://fal.ai/docs/documentation/model-apis/errors
- 2026-09-17 | fal concurrency starts at 2 requests per account and rises with purchased credit to 40; exceeding it queues. 429 `concurrent_requests_limit` signals the cap; back off exponentially | https://fal.ai/docs/model-apis/faq
- 2026-09-17 | Resend domains for this account: studioface.app is status=verified but region=us-east-1, NOT eu-west-1 (limeralda.com, mejoraweb.app and the-planning-app domain are all eu-west-1). Region is fixed at domain creation. The privacy page already discloses that some processors may handle data outside the EEA under RGPD safeguards, so this is disclosed rather than contradicted | GET https://api.resend.com/domains
- 2026-09-17 | Google Gemini image generation, PRIMARY: "When excluding elements, use semantic negative prompts that describe the scene positively (e.g. 'an empty street') rather than stating what not to include". nano-banana-2 is Gemini-family with no separate negative-conditioning input, so an exclusion list is just more subject matter in the prompt | https://ai.google.dev/gemini-api/docs/image-generation
- 2026-09-17 | Google recommended photorealistic template, PRIMARY, verbatim: "A photorealistic [type of shot] of a [subject description] in a [setting description]. [Description of the light]. Shot from a [camera angle] with a [lens type]." Google also asks for full-sentence prose, not comma-separated keyword lists, and for "hyper-specific details" | https://ai.google.dev/gemini-api/docs/image-generation
- 2026-09-17 | Google detail-preservation pattern, PRIMARY: "Ensure the woman's face and features remain completely unchanged" — an instruction to KEEP, which is the documented shape for identity preservation in an edit | https://ai.google.dev/gemini-api/docs/generate-content/image-generation
- 2026-09-17 | NOT DOCUMENTED ANYWHERE REACHABLE, do not treat as sourced: named lighting patterns (Rembrandt, butterfly, loop, split, clamshell) appear nowhere in Google's image docs; corporate-headshot conventions for lens range, camera height, backdrop and headroom could not be reached from any photography-industry source (B&H, PetaPixel, Format, Photography Life all 403/404/410/402). Our "85mm at f/2.8" is unverified folklore that happens to read plausibly | attempted and failed, recorded so nobody re-derives it
- 2026-09-17 | NOT DOCUMENTED: whether asking an edit model for a clothing change degrades face fidelity, and prompt language for over-smoothing / ethnicity drift / age drift / symmetry artefacts. Neither Google nor fal addresses any of it. Empirical question only | Google + fal docs, confirmed absent
- 2026-09-17 | NOT DOCUMENTED: any ideal prompt LENGTH for these models. Google says add hyper-specific detail and break complex scenes into steps, and states no ceiling. "Long prompts dilute" is unsourced in both directions | https://ai.google.dev/gemini-api/docs/image-generation
- 2026-09-17 | fal-ai/nano-banana-2/edit input confirmed again: prompt (required), image_urls (required list), num_images 1-4 default 1, seed, aspect_ratio enum default auto; 4:5 and resolution 1K are accepted values. The model page carries NO portrait, identity or wardrobe prompting guidance | Context7 /websites/fal_ai
- 2026-09-17 | GDPR Art. 9(1) special categories, PRIMARY text: racial or ethnic origin, political opinions, religious or philosophical beliefs, trade union membership, genetic data, biometric data for unique identification, health, sex life or sexual orientation. Gender identity is NOT named | https://gdpr-info.eu/art-9-gdpr/
- 2026-09-17 | Spain Ley 4/2023 Art. 44.3: rectifying the registered sex marker "en ningun caso podra estar condicionado a la previa exhibicion de informe medico o psicologico" — self-declaration is legally authoritative | https://www.boe.es/eli/es/l/2023/02/28/4
- 2026-09-17 | Prior art in this exact product category: Aragon.ai sells "Choice of 1 attire / 2 attires / All attires included" and asks the customer to "Select your attire and backgrounds". It does not ask a gender question | https://www.aragon.ai/
- 2026-09-17 | GOV.UK Design System does publish a sex/gender pattern (sex: Female/Male/Prefer not to say; gender identity as a separate optional question), but its stated purpose is EQUALITY MONITORING for public services, which is not a justification for driving a product feature | https://design-system.service.gov.uk/patterns/equality-information/
- 2026-09-18 | Cloud Tasks is AT-LEAST-ONCE, documented: "Cloud Tasks provides at least once delivery... In some rare circumstances, multiple task execution is possible", and handlers "should be designed to be idempotent to avoid harmful side effects" | https://docs.cloud.google.com/tasks/docs/dual-overview
- 2026-09-18 | Cloud Tasks HTTP target dispatch deadline: default 10 minutes, maximum 30. "The task attempt has failed if the app handler returns a non-2xx response code or Cloud Tasks does not receive response before the deadline" | https://docs.cloud.google.com/tasks/docs/creating-http-target-tasks + https://docs.cloud.google.com/tasks/docs/reference/rpc/google.cloud.tasks.v2
- 2026-09-18 | Cloud Run request timeout default 300s (max 3600). On timeout "the network connection to the service will be closed and an error 504 is returned" BUT "the container instance that served the request is not terminated. The container or code might keep processing the terminated request" | https://docs.cloud.google.com/run/docs/configuring/request-timeout
- 2026-09-18 | THE MECHANISM, measured on our own config: studioface-api timeoutSeconds=300, queue `generate` retryConfig maxAttempts=5 minBackoff=10s maxBackoff=300s maxDoublings=4. Cloud Run (300s) cuts BEFORE the Cloud Tasks deadline (600s), so a generation past five minutes gets a 504 while the container keeps generating, Cloud Tasks counts a failed attempt and retries after >=10s, and a second run of the same order begins. Up to 5 attempts = up to 20 paid fal images for one order. Combining the two vendor quotes above is an inference, not a single sentence from Google | gcloud run services describe + gcloud tasks queues describe
- 2026-09-18 | Cloud Tasks HTTP headers for detecting a retry: X-CloudTasks-TaskRetryCount (0 on the first attempt), X-CloudTasks-TaskExecutionCount (times the handler returned a response), plus X-CloudTasks-QueueName, -TaskName, -TaskETA, and conditionally -TaskPreviousResponse and -TaskRetryReason | https://docs.cloud.google.com/tasks/docs/creating-http-target-tasks
- 2026-09-18 | Google does NOT document a lease or claim pattern by name for Cloud Tasks; idempotent handlers is the documented mitigation and the implementation is left to you | https://docs.cloud.google.com/tasks/docs/dual-overview
- 2026-09-18 | fal billing lags request completion: "billable_units may be null if billing data is not yet available for a request", and serverless billing is per-second over the runner lifetime "including setup, idle time, active processing, and teardown". No delay figure is published. MEASURED here: balance read immediately after a batch said 9.0256, a later read with no intervening calls said 8.7056, so an immediate reading is a LOWER BOUND on spend | https://fal.ai/docs/api-reference/platform-apis/for-serverless + https://fal.ai/docs/documentation/serverless/faq + measured
- 2026-09-18 | NOT DOCUMENTED by Google: whether Cloud Tasks aborts the original in-flight request before dispatching a retry. Do not assume it does | Cloud Tasks docs, absent
- 2026-09-18 | GitHub Actions default environment variables: CI is "Always set to `true`. You can use this variable to differentiate when tests are being run locally or by GitHub Actions"; GITHUB_ACTIONS likewise. Used to skip a test that asserts developer-machine git config (core.hooksPath), which a CI checkout has none of and never needs | https://docs.github.com/en/actions/reference/workflows-and-actions/variables
- 2026-09-18 | Starlette StaticFiles/FileResponse sets content-length, last-modified and etag via set_stat_headers, and has NO code path that sets cache-control. Measured on production: neither the HTML nor a hashed chunk carried cache-control | https://github.com/encode/starlette/blob/master/starlette/responses.py + https://github.com/encode/starlette/blob/master/starlette/staticfiles.py
- 2026-09-18 | RFC 9111 4.2.2 heuristic freshness, verbatim: "a cache MAY assign a heuristic expiration time when an explicit time is not specified"; "If the response has a Last-Modified header field, caches are encouraged to use a heuristic expiration value that is no more than some fraction of the interval since that time. A typical setting of this fraction might be 10%." This is why a response with only etag+last-modified can be served stale | https://www.rfc-editor.org/rfc/rfc9111.html#section-4.2.2
- 2026-09-18 | `no-cache` means the response MAY be stored but must be revalidated with the origin before every reuse; `max-age=0, must-revalidate` is effectively equivalent and kept for HTTP/1.0 caches. MDN and web.dev both recommend `no-cache` for HTML and unversioned URLs | https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control + https://web.dev/articles/http-cache
- 2026-09-18 | Content-hashed assets: `public, max-age=31536000, immutable` is the exact directive Next.js documents for its own hashed build output ("These immutable files contain a SHA-hash in the file name, so they can be safely cached indefinitely"). In static-export mode there is NO Next.js server, so the filename hashing is portable but the header is not — emitting it is our FastAPI layer's job | https://nextjs.org/docs/app/guides/self-hosting + https://nextjs.org/docs/app/guides/static-exports
- 2026-09-18 | Cloud Run sets `Cache-Control: private` when a response carries Set-Cookie. NOT DOCUMENTED: any exhaustive statement that it never alters cache-control otherwise, and there is no default GFE edge caching of a Cloud Run service without Cloud CDN or a load balancer in front | https://docs.cloud.google.com/run/docs/container-contract
- 2026-09-18 | prefers-reduced-motion is Baseline WIDELY AVAILABLE, "available across browsers since January 2020". CORRECTION to the brief, which said July 2022 | https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-reduced-motion
- 2026-09-18 | CSS scroll-driven animations (animation-timeline: scroll()/view()) are MDN Baseline "Limited availability", NOT Baseline. MDN browser-compat-data: Chrome 115, Safari 26, Firefox "preview" — i.e. not in Firefox stable. NO global percentage is cited here: caniuse is JS-rendered, the figure fetched (~87%) disagreed with the brief's 82.6%, and its "Firefox 159" contradicts MDN's own preview status, so the number is treated as unverified | https://developer.mozilla.org/en-US/docs/Web/CSS/animation-timeline + https://github.com/mdn/browser-compat-data/blob/main/css/properties/animation-timeline.json
- 2026-09-18 | IntersectionObserver is Baseline "Widely available", across browsers since March 2019 | https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API
- 2026-09-18 | Bundle sizes from bundlephobia, accessed today: gsap 3.15.0 main entry 70,605 B raw / 27,350 B gzip; motion 13.4.0 142,673 B raw / 47,708 B gzip. CORRECTION to the brief's "GSAP ScrollTrigger about 23 KB, Motion about 32 KB". NOT VERIFIED: a combined GSAP+ScrollTrigger figure — ScrollTrigger is a separate entry point and bundlephobia returned the core-only number, so 27.35 KB must not be quoted as "with ScrollTrigger" | https://bundlephobia.com/package/gsap + https://bundlephobia.com/package/motion
- 2026-09-18 | web.dev, verbatim: "Where possible, restrict animations to `opacity` and `transform` to keep animations on the compositing stage of the rendering path" and "Avoid any property that triggers layout or paint unless it is absolutely necessary" | https://web.dev/articles/animations-guide
- 2026-09-18 | NOT VERIFIED and deliberately not cited: any authoritative numeric upper bound for UI animation duration. Material Design 2 and 3 are JS-rendered and unfetchable; MDN and web.dev give example values but no normative recommendation. Our 400ms ceiling is a PROJECT DECISION, not a standard, and is labelled as such wherever it is asserted | attempted m2.material.io, m3.material.io, MDN CSS transitions, web.dev animations guide
- 2026-09-18 | GA4 custom event names: case sensitive (`my_event` != `My_Event`); must START WITH A LETTER; letters, numbers and underscores only, no spaces; `query_id` is a reserved prefix for web streams; a list of reserved names (page_view, session_start, click, ...) may not be reused | https://support.google.com/analytics/answer/13316687
- 2026-09-18 | GA4 web-stream collection limits: event name 40 characters; NO limit on the number of distinctly named events for web streams; 25 event parameters per event; parameter name 40 characters; parameter value 100 characters (page_title 300, page_referrer 420, page_location 1000) | https://support.google.com/analytics/answer/9267744
- 2026-09-18 | LCP "good" threshold is 2.5 SECONDS at the 75th percentile, segmented across mobile and desktop. Eligible LCP elements explicitly include <img>, <image> in SVG, <video>, elements with a url() background, and block-level elements containing text. NOTHING requires the LCP element to be text — our "the LCP element must be the H1" rule was a proxy, not a requirement | https://web.dev/articles/lcp
- 2026-09-18 | NOT VERIFIED: the Baseline status of the `fetchpriority` ATTRIBUTE on <img>. MDN's "Baseline Widely available, since July 2015" badge on that page describes the <img> element, and the page warns "Some parts of this feature may have varying levels of support". Not used in the code for that reason | https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/img
- 2026-09-18 | CORRECTION to the line above: the `fetchpriority` attribute on <img> IS supported everywhere. MDN browser-compat-data: Chrome 101, Edge mirror, Firefox 132, Safari 17.2, Safari iOS mirror, Chrome Android mirror; experimental false, standard_track true, deprecated false. It is also a HINT, so an older browser ignores it with no downside. The earlier "not verified" was me reading the element's Baseline badge instead of the attribute's compat data | https://raw.githubusercontent.com/mdn/browser-compat-data/main/html/elements/img.json
- 2026-09-18 | Cloudflare Turnstile explicit rendering with an async script must NOT call turnstile.render() directly on mount. Documented patterns: `?onload=<fn>` in the script URL, `turnstile.ready(cb)`, or window.onload. A bare `if (!window.turnstile) return;` on mount is a race the widget never recovers from | https://developers.cloudflare.com/turnstile/get-started/client-side-rendering/
- 2026-09-18 | MEASURED against production studioface.app: window.turnstile is `undefined` at DOMContentLoaded (when React runs mount effects) and an object with .render by t=500ms. The widget container `<div class="min-h-[65px]">` stayed at 0 children and `input[name=cf-turnstile-response]` stayed at 0 for the full 10s observed. The token is therefore always the empty string, /api/preview answers 403 "turnstile", and no visitor can obtain a free preview | live probe, six time points, uninstrumented
- 2026-09-18 | Cloudflare Turnstile `turnstile.ready()` is INVALID when the api.js tag is async or defer. Their runtime throws it out loud: "TurnstileError: [Cloudflare Turnstile] Remove async/defer from the Turnstile api.js script tag before using turnstile.ready()". Next injects the tag async (`strategy="afterInteractive"`), so the first documented pattern does not apply to us. Once `window.turnstile` is an object the script has executed and render() is safe — the same guarantee their `?onload=` callback gives | live console error from challenges.cloudflare.com api.js, reproduced locally
- 2026-09-18 | Cloudflare Turnstile PUBLIC test site keys (always pass / always block / always challenge): 1x00000000000000000000AA, 1x00000000000000000000BB, 2x00000000000000000000AB, 2x00000000000000000000BB, 3x00000000000000000000FF. Public by definition, which is why shipping one is dangerous rather than merely untidy: the widget looks present and the challenge is a no-op. tests/test_turnstile_widget.py fails if any appears in frontend/out | https://developers.cloudflare.com/turnstile/troubleshooting/testing/
- 2026-09-18 | WCAG 2.2 SC 2.5.8 Target Size (Minimum) is Level AA at 24x24 CSS pixels, with five exceptions. The third is Inline: "The target is in a sentence or its size is otherwise constrained by the line-height of non-target text". SC 2.5.5 Target Size (Enhanced) is Level AAA. Our 44px floor is a PLATFORM guideline, stricter than WCAG AA, and it does not apply to a link sitting inside a paragraph | https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html
- 2026-09-18 | Turnstile's widget language default is `auto`, which follows the visitor's browser language. MEASURED against production with two browser locales: es-ES renders "Verifique que es un ser humano / Privacidad - Ayuda", en-US renders "Verify you are human". So NO explicit language option is set: the default is already correct for the audience, and forcing es-es would only affect visitors whose browser is not Spanish. An earlier observation of the English widget was an artefact of the probe's locale, not a defect | https://developers.cloudflare.com/turnstile/reference/supported-languages/ + live probe, two locales
- 2026-09-18 | prefers-reduced-motion, BOTH dates, because they mean different things and quoting one as the other is how the skill went wrong. INTEROPERABLE (Baseline "Newly available") 2020-01-15: MDN browser-compat-data gives Chrome 74, Firefox 63, Safari 10.1, Edge "mirror" — and Edge mirrors from 79, the first Chromium Edge, whose release_date in BCD browsers/edge.json is "2020-01-15". Edge was the last core browser, so that is the interoperable date. BASELINE WIDELY AVAILABLE 2022-07-15: web.dev defines Widely available as "30 months have passed since the newly interoperable date", and 2020-01-15 + 30 months is 2022-07-15. The core browser set is Chrome (desktop and Android), Edge, Firefox (desktop and Android), Safari (macOS and iOS) | https://raw.githubusercontent.com/mdn/browser-compat-data/main/css/at-rules/media.json + https://raw.githubusercontent.com/mdn/browser-compat-data/main/browsers/edge.json + https://web.dev/baseline
- 2026-09-18 | THIRD PRODUCTION OUTAGE, measured not guessed. `curl -i -X OPTIONS https://api.studioface.app/api/preview -H "Origin: https://studioface.app" -H "Access-Control-Request-Method: POST"` returns **405 Method Not Allowed with NO access-control-allow-origin header**. Same on the apex. The app installs GZipMiddleware and nothing else — no CORSMiddleware — and Starlette only answers OPTIONS on a route when CORSMiddleware is present, so a preflight to any /api path 405s | live curl, both hostnames, and `grep add_middleware app/`
- 2026-09-18 | CORS semantics that decide the fix. A "simple request" needs method GET/HEAD/POST, safelisted headers, and Content-Type of application/x-www-form-urlencoded, multipart/form-data or text/plain — anything else PREFLIGHTS. So /api/checkout (application/json) preflights and dies on the 405; /api/preview (multipart/form-data) does NOT preflight, but "if the server does not respond with Access-Control-Allow-Origin ... the response would be ignored and not made available to the web content", so fetch rejects and the visitor sees "No hemos podido conectar." Both paths dead, by two different mechanisms. **Same-origin requests are not subject to CORS at all** — which is why the fix is to remove the cross-origin call rather than to permit it | https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS
- 2026-09-18 | Both hostnames are the SAME Cloud Run service and the same FastAPI process: `https://studioface.app/health` and `https://api.studioface.app/health` both return {"ok":true,"killswitch":false}, and app/main.py mounts the static export last so /api, /internal and /health win. A relative path from the apex therefore reaches the identical handler with no CORS involved | live curl, both hostnames
- 2026-09-18 | DMARC for studioface.app, read with DNS-over-HTTPS from two independent resolvers and identical from both: `v=DMARC1; p=quarantine; rua=mailto:kevinleonjouvin@gmail.com`. CORRECTION to the brief, which asked whether the policy should move "toward quarantine" — it is ALREADY p=quarantine. The open question is p=reject, and the missing tags are pct, sp, adkim, aspf, ruf and fo, all of which fall back to their defaults | https://dns.google/resolve?name=_dmarc.studioface.app&type=TXT + https://cloudflare-dns.com/dns-query (accept: application/dns-json)
- 2026-09-18 | SPF for studioface.app: `v=spf1 include:_spf.mx.cloudflare.net include:amazonses.com ~all`. Resend sends through Amazon SES, which is why amazonses.com is the authorising include; _spf.mx.cloudflare.net is Cloudflare Email Routing for INBOUND. The qualifier is ~all (softfail), not -all | https://dns.google/resolve?name=studioface.app&type=TXT
- 2026-09-18 | DKIM: `resend._domainkey.studioface.app` exists and publishes a DKIM RSA public key (the p= value is omitted here: it is public by definition, published in DNS for every receiver to read, but its entropy trips secret scanning and a scanner that has to be argued with is a scanner people learn to bypass). Selectors `send` and `_domainkey` are NXDOMAIN, so Resend's is the only signing selector on this domain | https://dns.google/resolve?name=resend._domainkey.studioface.app&type=TXT
- 2026-09-18 | AEPD cookie-banner requirements (Guía sobre el uso de las cookies, MAYO 2024): accepting and rejecting must be offered AT THE SAME TIME, AT THE SAME LEVEL and WITH THE SAME VISIBILITY, in a prominent place and format, and it must not be more complicated to reject than to accept. Withdrawal must be as easy as giving consent. NOTHING in the requirement sets a minimum banner SIZE, mandates a paragraph of explanatory prose in the first layer, or forbids a compact presentation — the requirement is parity and prominence, not area. Our banner is 153px of a 390x844 first viewport (18%) and its two buttons are already at the same level and visibility, so it is COMPLIANT and merely larger than it needs to be | https://www.aepd.es/guias/guia-cookies.pdf + https://www.aepd.es/prensa-y-comunicacion/notas-de-prensa/aepd-actualiza-guia-cookies-para-adaptarla-a-nuevas-directrices-cepd
- 2026-09-18 | MEASURED: https://studioface.app serves ZERO security headers. `curl -I` returns content-type, accept-ranges, last-modified, etag, cache-control, x-cloud-trace-context, content-length, date, server and nothing else. No Strict-Transport-Security, no X-Content-Type-Options, no Referrer-Policy, no Content-Security-Policy | live curl -I, 18 Sep
- 2026-09-18 | Cloudflare Turnstile CSP requirements, from Cloudflare's own reference: `script-src: https://challenges.cloudflare.com` and `frame-src: https://challenges.cloudflare.com`. Pre-clearance additionally needs `connect-src 'self'`. No style-src requirement is stated | https://developers.cloudflare.com/turnstile/reference/content-security-policy/
- 2026-09-18 | Stripe CSP requirements, from Stripe's own security guide. For CHECKOUT: connect-src/frame-src/script-src `https://checkout.stripe.com`, img-src `https://*.stripe.com`. For Stripe.js: frame-src `https://*.js.stripe.com https://js.stripe.com https://hooks.stripe.com`, script-src the same, connect-src `https://api.stripe.com`. WE NEED NONE OF THESE: we use Checkout in REDIRECT mode (`window.location.href = data.url` in upload-form.tsx) and load no Stripe script on our origin, so the browser leaves studioface.app entirely before any Stripe code runs. A grep for `js.stripe.com` and `Stripe(` across frontend/src returns nothing | https://docs.stripe.com/security/guide
- 2026-09-18 | MEASURED origins our own pages actually request, three routes, by resource type: document/script/image/xhr/fetch challenges.cloudflare.com (+ brunhild.challenges.cloudflare.com); script www.googletagmanager.com; fetch region1.google-analytics.com; font, stylesheet, and everything else studioface.app. Fonts are SELF-HOSTED — next/font/google inlines them at build time, so no fonts.gstatic.com is needed | live Playwright, /, /recuperar/, /legal/cookies/
- 2026-09-18 | HSTS: `Strict-Transport-Security: max-age=<seconds>; includeSubDomains; preload`. MDN's recommended value is `max-age=31536000; includeSubDomains`; preload requires max-age at least 31536000 AND includeSubDomains. The header is IGNORED when served over plain HTTP, by design, so that a MITM cannot add or expire it | https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Strict-Transport-Security
- 2026-09-18 | Branch protection on main, applied and TESTED rather than assumed. GitHub's docs describe branch restrictions as available "in public repositories owned by a GitHub Free organization and in all repositories owned by an organization using GitHub Team or GitHub Enterprise Cloud"; this repo is USER-owned and PRIVATE, and the API accepted the rule anyway, so the doc's org-centred wording does not describe the current limit. Now set: allow_force_pushes false, allow_deletions false, required_linear_history true, enforce_admins false | https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches + live gh api PUT
- 2026-09-18 | REQUIRED STATUS CHECKS WERE TRIED AND DELIBERATELY REMOVED. GitHub's docs: "After all required status checks pass, any commits must either be pushed to another branch and then merged or pushed directly to the protected branch" — they can block direct pushes. Measured with a real push while ci/design/emulator were required: it SUCCEEDED with `remote: - 3 of 3 required status checks are expected.`, because enforce_admins is false and the pusher is an admin. So for us they warn and never block — except for self-heal, which pushes as a GitHub App and is not an admin, AND which by definition pushes when the checks are RED. Requiring green checks to push the fix for a red check is circular and would disable the healer. Blocking force-push and deletion is kept; gating checks needs a RULESET with the healer as a bypass actor, which is a separate change | live push against a protected branch, 18 Sep
- 2026-09-18 | MEASURED: the deployer service account sa-studioface-deployer@studio-face-fresh-start holds roles/owner on the project, granted by infra/ci.tf. Owner permits reading every secret's VALUE, deleting the Firestore database, deleting the buckets holding customers' photographs, granting itself any role, and removing Kevin's own access — and the credential is reachable from a GitHub Actions workflow | gcloud projects get-iam-policy studio-face-fresh-start
- 2026-09-18 | The eleven roles Terraform actually needs, derived from the resource types declared in infra/*.tf and each name confirmed to exist with `gcloud iam roles describe`: serviceusage.serviceUsageAdmin, resourcemanager.projectIamAdmin, iam.serviceAccountAdmin, iam.workloadIdentityPoolAdmin, secretmanager.admin, storage.admin, datastore.owner, cloudtasks.admin, run.admin, artifactregistry.admin, pubsub.admin. Granted additively as phase one; roles/owner removal is phase two, separated because Terraform uses these permissions to change these permissions | live gcloud iam roles describe, twelve names
- 2026-09-18 | GitHub Actions permissions: "If you specify the access for any of these permissions, all of those that are not specified are set to `none`" — so an explicit block is already a whitelist. `id-token: write` grants fetching an OIDC token; `pull-requests: write` grants working with pull requests. self-heal.yml held both and uses neither: there is no google-github-actions/auth step and no `gh pr` anywhere in the file. Removed; contents/actions/issues kept because it pushes the fix, reads the failed run's logs, and opens "needs Kevin" | https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#permissions
- 2026-09-18 | MEASURED: the production image installed NOTHING pinned. Dockerfile ran `pip install --no-cache-dir .` against a pyproject whose `dependencies` list names fastapi, uvicorn, python-multipart, httpx, fal-client, stripe, resend, google-cloud-firestore, google-cloud-storage, google-cloud-tasks, pillow and pillow-heif with no version constraint on any of them. Every build resolved afresh. Now locked: `uv pip compile pyproject.toml --generate-hashes --python-version 3.12 --python-platform linux -o requirements.lock` yields 50 packages and 1011 sha256 hashes, installed with --require-hashes | live uv 0.10.9, and the Dockerfile before/after
- 2026-09-18 | Stripe test and live are separate object spaces, verbatim: "Cada modo tiene su propio conjunto de claves de API, y los objetos de un modo no son accesibles desde el otro. Por ejemplo, un objeto de producto del entorno de prueba no puede formar parte de un pago en modo activo." Test keys are pk_test_/rk_test_/sk_test_, live are pk_live_/rk_live_/sk_live_. Webhook signing secrets are NOT API keys and are per-endpoint | https://docs.stripe.com/keys
- 2026-09-18 | Stripe refund reconciliation events, verbatim from the Bizum page: "Los reembolsos de pagos Bizum son asíncronos y tardan hasta 5 minutos en completarse. Stripe te notifica el estatus final reembolso usando el evento webhook `refund.updated` o `refund.failed`. Cuando un reembolso se realiza correctamente, el estatus del objeto Refund pasa a `succeeded`. Si un reembolso falla (el estatus del objeto Refund pasa a `failed`), entonces devolvemos el importe a tu saldo de Stripe y debes buscar una forma alternativa de reembolsar a tu cliente." So GO-LIVE step 11b names exactly the right two events | https://docs.stripe.com/payments/bizum
- 2026-09-18 | Bizum IS available to a Spanish merchant: ES is a listed business location, presentment currency EUR only, Checkout supported (NOT in subscription or setup mode — we use one-time payment mode), recurring payments No, manual capture No, min charge 0.50 EUR, max 5,000.00 EUR, dispute window 120 days, refund period 395 days. AI headshots is not in the prohibited category list | https://docs.stripe.com/payments/bizum
- 2026-09-18 | Bizum REQUIREMENTS we do not yet meet, verbatim: "Individuals and sole proprietors: Provide a valid personal identification number using the individual.id_number field. Spain: Provide your DNI or NIE if you are a foreign resident." and "You must also set the business_type on your account." Plus: "The Bizum payments capability stays in a `pending` state until compliance with Bizum onboarding requirements is verified." Our trader NIF is Z3714124-C, a NIE, so this is the individual/sole-trader path and needs individual.id_number plus business_type set in the Stripe account — account configuration, which is Kevin's | https://docs.stripe.com/payments/bizum
- 2026-09-19 | Stripe Checkout Session `payment_status`, verbatim: "The payment status of the Checkout Session, one of `paid`, `unpaid`, or `no_payment_required`. You can use this value to decide when to fulfill your customer's order." `paid` = "The payment funds are available in your account." So the ONLY condition that proves payment for our one-time `mode: payment` session is `payment_status == "paid"` | https://docs.stripe.com/api/checkout/sessions/object
- 2026-09-19 | Stripe `consent_collection.terms_of_service`, verbatim: "If set to `required`, it requires customers to accept the terms of service before being able to pay." Enum `none`/`required`. The field description carries NO mode restriction either way, so its availability for `mode: payment` is available-by-absence-of-restriction rather than an explicit confirmation | https://docs.stripe.com/api/checkout/sessions/object
- 2026-09-19 | FastAPI: `FastAPI(openapi_url=None)` alone disables the schema AND both UIs — "If `openapi_url` is set to `None`, docs are automatically disabled". `docs_url=None, redoc_url=None` disables only the UIs and leaves the schema served | https://fastapi.tiangolo.com/reference/fastapi
- 2026-09-19 | Google Pub/Sub push authentication, what the RECEIVING application must verify (not what Cloud Run IAM can do): the JWT arrives in the `Authorization` header as "Bearer [JWT]"; verify the signature against Google's public certificates, check the issuer equals "https://accounts.google.com", check the `aud` claim equals the audience configured on the subscription, and check the token's email equals the expected push service account with `email_verified` true | https://docs.cloud.google.com/pubsub/docs/authenticate-push-subscriptions
- 2026-09-19 | gtag.js get command, verbatim: "gtag('get', '<target>', '<field_name>', callback)" with field_name "one of the following values: client_id, session_id, session_number, gclid" | https://developers.google.com/tag-platform/gtagjs/reference
- 2026-09-19 | NOT CONFIRMED: what `client_id` / `session_id` return, or whether a client_id exists at all, while `analytics_storage` is DENIED under Consent Mode v2. Both the consent devguide and the advanced consent guide were checked and are silent on it; they describe cookieless URL passthrough generally but never the value or existence of client_id in that state. Code must therefore treat both as OPTIONAL and omit them when absent, not assume either outcome | https://developers.google.com/tag-platform/devguides/consent (checked, silent)
- 2026-09-19 | NOT CONFIRMED: whether GA4 deduplicates a `purchase` sent from gtag.js and one sent via the Measurement Protocol with the same `transaction_id`. The events reference says only "The `transaction_id` parameter helps you avoid getting duplicate events for a purchase", with no statement about cross-source (gtag vs MP) behaviour. DESIGN CONSEQUENCE: one sender only — the Stripe webhook — and no browser `purchase` | https://developers.google.com/analytics/devguides/collection/ga4/reference/events (checked, silent on MP-vs-gtag)
- 2026-09-19 | CONFIRMED verbatim, Google Ads Help: "Starting June 15, 2026, offline conversions import and enhanced conversions for leads uploads will be migrated to the Data Manager API and blocked in the Google Ads API." | https://support.google.com/google-ads/answer/2998031
- 2026-09-19 | PARTIALLY CONFIRMED for the conversion import schema. Data Manager is named — "The fastest and easiest way to set up or upgrade to enhanced conversions for leads is by using Data Manager, where GCLID and user-provided data can both be used as match keys for data imports" — but Google's page does NOT list Google Sheets or file upload as no-code sources by name, and does NOT give Data Manager's required columns. The LEGACY file-import columns are documented (Google Click ID, Conversion Name, Conversion Time in a timezone-qualified format such as `yyyy-MM-dd HH:mm:ss+z`, optional Conversion Value / Currency ISO 4217 / Order ID) and "Incorrectly formatted or missing time zone information in the 'Conversion Time' values is a frequent cause of upload errors" — but that is the pre-migration mechanism and is NOT confirmed to equal the Data Manager schema | https://support.google.com/google-ads/answer/7014069 (legacy) + https://support.google.com/google-ads/answer/2998031
- 2026-09-19 | CORRECTION to the 2026-09-17 line above, which recorded only www.google-analytics.com for the Measurement Protocol. Google documents an EU collection endpoint, verbatim: "If you want your data to be collected in the EU, use the following endpoint instead: https://region1.google-analytics.com/mp/collect" | https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference
- 2026-09-19 | Google CSP requirements for GA4 and Google Ads, from Google's own tag-platform security guide, fetched today. GA4 WITH ADS FEATURES: `script-src-elem https://www.googletagmanager.com`; `img-src https://www.googletagmanager.com https://*.google-analytics.com https://*.google.com https://*.google.<TLD> https://*.g.doubleclick.net`; `connect-src` the same plus `https://pagead2.googlesyndication.com`; `frame-src https://www.googletagmanager.com`. GOOGLE ADS: `script-src-elem https://www.googletagmanager.com https://www.googleadservices.com https://www.google.com`; `img-src` adds `https://googleads.g.doubleclick.net https://pagead2.googlesyndication.com`; `connect-src` adds `https://ad.doubleclick.net`; `frame-src https://www.googletagmanager.com`. TWO THINGS THE GUIDE DOES NOT SAY OUT LOUD: (a) it specifies `script-src-elem`, and we send only `script-src`, which CSP Level 3 falls back to - correct today, and silently broken the day anyone adds a `script-src-elem`; (b) `https://*.google.<TLD>` is prose, not a source expression - `<TLD>` is not a domain, so writing it literally yields a source that can never match and no parse error. We name the properties we serve: `*.google.com` and `*.google.es` | https://developers.google.com/tag-platform/security/guides/csp
- 2026-09-19 | R2a Google Product structured data: the only REQUIRED property is `name`; the page must additionally carry at least one of `review`, `aggregateRating` or `offers`. Inside `offers`, `price` (or `priceSpecification.price`) is required; `priceCurrency` (ISO 4217) and `availability` are recommended, and `priceCurrency` is required for Merchant Listing eligibility. Product rich results "only support pages that focus on a single product (or multiple variants of the same product)" and exclude category pages, which fits our one-product page. NOT CONFIRMED: nothing on the page distinguishes a service from a physical product, so Product for a 19,99 EUR service is an inference from the single-product rule, not a documented statement | https://developers.google.com/search/docs/appearance/structured-data/product-snippet
- 2026-09-19 | R2b FAQPage rich results: Google's own changelog, September 2023, verbatim: "Updated the FAQ structured data documentation to state that the feature is only shown for well-known, authoritative government and health websites." A later changelog entry, June 2026, verbatim: "Removed documentation for the FAQ rich result feature." The dedicated FAQPage documentation URL no longer resolves to its own page. DECISION: we do not ship FAQPage JSON-LD. A Spanish commercial headshot site is neither government nor health, so it was ineligible from Sept 2023 and the feature's documentation is now gone. NOT CONFIRMED: the day of month of either entry - repeated fetches disagreed at day precision while the quoted sentences reproduced identically, so only month and year are trusted | https://developers.google.com/search/updates
- 2026-09-19 | R2c JSON-LD placement: Google's structured data intro describes JSON-LD as embedded "in a `<script>` tag in the `<head>` and `<body>` elements", so either is allowed. Verbatim on client-side injection: "Google can read JSON-LD data when it is dynamically injected into the page's contents, such as by JavaScript code or embedded widgets in your content management system." So it need not be in the served HTML - but we put it in the static export anyway, because our export is static and there is no reason to depend on a rendering pass | https://developers.google.com/search/docs/appearance/structured-data/intro-structured-data
- 2026-09-19 | R3a Core Web Vitals thresholds, each assessed at the 75th percentile of page loads and segmented across mobile and desktop: LCP good <= 2.5s, poor > 4.0s; CLS good <= 0.1, poor > 0.25; INP good <= 200ms, poor > 500ms | https://web.dev/articles/lcp , https://web.dev/articles/cls , https://web.dev/articles/inp
- 2026-09-19 | R3b INP replaced FID as a Core Web Vital on 12 March 2024, per web.dev's own announcement | https://web.dev/blog/inp-cwv-march-12
- 2026-09-19 | R5a Google responsive search ad limits, re-confirming F10: headline 30 characters, up to 15 headlines, minimum 3; description 90 characters, up to 4, minimum 2; path fields 15 characters each. Double-width languages (Korean, Japanese, Chinese) count each character as two - irrelevant for es-ES. NOT CONFIRMED: the number of path fields; the page states the 15-character limit but the fetched text did not state the count, so scripts/check_ad_copy.py's assumption of two path fields rests on the brief's F10, not on this page | https://support.google.com/google-ads/answer/7684791
- 2026-09-19 | R5b Exact match (positive): "Ads may show on searches that have the same meaning or same intent as the keyword", written [like this] | https://support.google.com/google-ads/answer/7478529
- 2026-09-19 | R5b Negative keyword match types: negative BROAD blocks when the search contains all the negative terms in any order; negative PHRASE blocks when it contains the terms in the same order; negative EXACT blocks only the exact terms in order with no extra words. THE TRAP, documented: negatives do not behave like positives - "Negative keywords won't match to close variants or other expansions" and "you'll need to add synonyms and singular or plural versions if you want to exclude them". Casing and misspellings are handled automatically, plurals and synonyms are NOT. So the M8 negative list must spell out every plural and synonym itself, or it silently fails to block the generic image-generation searches that took 41% of version 1 spend | https://support.google.com/google-ads/answer/2453972
- 2026-09-19 | R5c Ads policy "Destination not working": the destination must function - no under-construction or error pages, and it must not return an HTTP error (403, 404, 500) to Google's AdsBot crawlers, on common browsers and devices, globally. Directly relevant here: /robots.txt and /sitemap.xml currently 404 (F7), and AdsBot must not be blocked | https://support.google.com/adspolicy/answer/16428019
- 2026-09-19 | R5c Ads policy "Destination mismatch": the display URL's domain must match the final URL the visitor actually lands on; a final URL or tracking template may not redirect to a different domain. Relevant because /api/gracias redirects, and because any ads landing page must stay on studioface.app | https://support.google.com/adspolicy/answer/16428020
- 2026-09-19 | R5c Ads policy "Destination requirements" (parent): destinations "must offer unique value to users and be functional, useful, and easy to navigate" | https://support.google.com/adspolicy/answer/6368661
- 2026-09-19 | R6a/R6b AEPD cookie guidance on the visual weight of accept versus reject: NOT CONFIRMED. https://www.aepd.es/guias/guia-cookies.pdf returns 200 but its extractable text was Adobe Illustrator font and logo metadata only; the guias listing did not carry the guide and /normativa/guias returned 503. No EDPB or CNIL text was substituted for it. Unit U4 applies either way, per the brief. Follow-up needs a PDF parser that handles this file, or an HTML mirror of the guide | https://www.aepd.es/guias/guia-cookies.pdf
- 2026-09-19 | R4f Playwright browsers installed on this machine, probed by launching each: chromium INSTALLED (140.0.7339.16); webkit NOT installed; firefox NOT installed. So B0.2's "WebKit if R4 found it installed" resolves to Chromium only, and no WebKit evidence exists for this session | (measured locally, no URL)
- 2026-09-19 | U2 latency gate, measured from Cloud Run request logs for studioface-api, 30-day window. FREE PREVIEW (POST /api/preview, status 200): only TWO exist in the whole retained window - 10.043646817s and 12.300008811s, median 11.17s. The other 46 requests are 41x 405 (the canary's OPTIONS preflight) and 5x 403 (Turnstile refusals). FULL GENERATION (/internal/generate/<order>, status 200): three runs - 14.563141830s, 19.777507994s, 28.982969111s, median 19.78s. Both medians are an order of magnitude below U2's 120-second threshold and the slowest run observed is 28.98s, a 4x margin, so "en dos minutos" stays in the H1, the page title and docs/ads/rsa.json. NOT CONFIRMED at the sample size the brief asks for: there are 2 previews, not 20, and generating 5 fresh ones needs a human to solve a Turnstile challenge in production and spends fal credits - see "needs Kevin" in HANDOFF. The decision does not turn on it | measured locally with gcloud logging read, no URL
- 2026-09-19 | A4.1a Turnstile `turnstile.reset(widgetId)` takes the widget id that `turnstile.render()` returned; "To reset the widget if the given widget timed out or expired, you can use the function". Same shape for `getResponse(widgetId)`, `remove(widgetId)`, `isExpired(widgetId)`. No return value is documented for reset | https://developers.cloudflare.com/turnstile/get-started/client-side-rendering/
- 2026-09-19 | A4.1b Turnstile token lifetime and replay, verbatim: "The validation token issued by Turnstile is valid for five minutes. If a user submits the form after this period, the token is considered expired." and "Each token can only be validated once. A replayed token will be rejected with the `timeout-or-duplicate` error code." THIS IS I3: with no `turnstile.reset` anywhere in the bundle, every retry after a failed preview replays a spent token and is refused, until the page is reloaded | https://developers.cloudflare.com/turnstile/get-started/server-side-validation/
- 2026-09-19 | A4.1d Turnstile dummy keys, all public constants. SITE keys: 1x..AA always passes (visible), 2x..AB always fails (visible), 1x..BB always passes (invisible), 2x..BB always fails (invisible), 3x..FF forces an interactive challenge. SECRET keys: 1x..AA always passes, 2x..AA always fails, 3x..AA returns "token already spent". Test site keys emit a token shaped `XXXX.DUMMY.TOKEN.XXXX`, and a test secret accepts only a dummy token while a real secret rejects one - so the pair must never be mixed with a production key | https://developers.cloudflare.com/turnstile/troubleshooting/testing/
- 2026-09-19 | A4.2a Stripe fulfilment, verbatim, and it CORRECTS an assumption in this project: "Listening to webhooks is required to make sure you always trigger fulfillment for every payment, but webhooks can sometimes be delayed. To optimize your payment flow and guarantee immediate fulfillment when your customer is present, trigger fulfillment from your landing page as well." and "You can't rely on triggering fulfillment only from your Checkout landing page, because your customers aren't guaranteed to visit that page." Both paths fulfil, which is what unit C2(a) asks for | https://docs.stripe.com/checkout/fulfillment.md?payment-ui=stripe-hosted
- 2026-09-19 | A4.2b Checkout Session `payment_status` is one of `paid`, `unpaid`, `no_payment_required`; `status` is one of `open`, `complete`, `expired`, where `complete` means "The checkout session is complete. Payment processing may still be in progress" | https://docs.stripe.com/api/checkout/sessions/object.md
- 2026-09-19 | A4.2b(2) Stripe's OWN fulfilment gate is `payment_status != 'unpaid'`, not `== 'paid'`: its reference implementation fulfils for `paid` AND `no_payment_required`. This corrects the stricter rule written into /api/gracias earlier today - a 100%-off coupon produces `no_payment_required` and is a legitimate order that would have been refused. Brief C2(b) says the same: refuse when unpaid or absent | https://docs.stripe.com/checkout/fulfillment.md?payment-ui=stripe-hosted
- 2026-09-19 | A4.2c Stripe mandates idempotent fulfilment in prose, not through an API feature: "Perform fulfillment only once per payment. Because of how this integration and the internet work, your `fulfill_checkout` function might be called multiple times, possibly concurrently, for the same Checkout Session." It must "Make sure fulfillment hasn't already been performed for this Checkout Session" and record the fulfilment status | https://docs.stripe.com/checkout/fulfillment.md?payment-ui=stripe-hosted
- 2026-09-19 | A4.2d stripe-python v15.0.0 breaking change, THE ROOT CAUSE OF I2: "`StripeObject` no longer inherits from `dict`, so any `dict` methods will no longer exist, including `.get()` and notably, `.items()`." The supported replacement is `some_obj.to_dict()`, "which recursively dumps all stripe-provided classes into native Python types". So `dict(stripe.checkout.Session.retrieve(id))` is wrong on v15 and `.to_dict()` is right. NOT CONFIRMED: the exact TypeError text is documented nowhere, so no test asserts on the message | https://github.com/stripe/stripe-python/wiki/Migration-guide-for-v15
- 2026-09-19 | A4.2d(2) `stripe.error` was removed in stripe-python v13.0.0 ("Removed deprecated module shims... Specifically, we removed: ... `stripe.error`"), so `except stripe.error.InvalidRequestError` is the wrong path and `stripe.InvalidRequestError` is the right one. MEASURED CAVEAT: on 15.6.1 in this repository `stripe.error.InvalidRequestError` still resolves to `<class 'stripe._error.InvalidRequestError'>`, so the shim outlived the changelog entry - which is why I2 presented as a TypeError escaping the handler rather than an AttributeError | https://raw.githubusercontent.com/stripe/stripe-python/master/CHANGELOG.md
- 2026-09-19 | A4.2e Stripe-Signature computed by hand: one header line of comma-separated `prefix=value` pairs, `t=<unix timestamp>,v1=<hex hmac>`. The signed payload is "the timestamp (as a string), the character `.`, the actual JSON payload (that is, the request body)", and the signature is "an HMAC with the SHA256 hash function... the endpoint's signing secret as the key". Only scheme `v1` counts; the default tolerance in Stripe's own libraries is five minutes and "Do not use a tolerance value of `0`" | https://docs.stripe.com/webhooks.md
- 2026-09-19 | A4.3a fal result hostnames are NOT stable and must not be pinned: fal's own docs show `v3b.fal.media`, `v3.fal.media` and `v2.fal.media`, and describe a fallback chain - "If the primary endpoint (v3.fal.media) is unavailable, the SDK automatically falls back to fal.media, then to the REST API". No page states that a hostname is guaranteed. A second and stronger reason not to allowlist fal in img-src for unit C1 | https://fal.ai/docs/documentation/model-apis/fal-cdn
- 2026-09-19 | A4.3b fal retention: generated files are "stored on the fal CDN and available for at least 7 days by default" and their URLs are "publicly accessible - anyone with the URL can access the file until it expires". So the behaviour before C1 left a customer's generated face publicly reachable on a third-party CDN for at least a week. fal itself recommends downloading for long-term storage | https://fal.ai/docs/documentation/model-apis/faq
- 2026-09-19 | A4.3c fal content-policy rejection, matching the production traceback of 19 Sep 13:27:18 exactly: HTTP 422, non-retryable, body `{"detail": [...]}` with each item carrying `loc`, `msg`, `type`, `url`; the policy case is `"type": "content_policy_violation"`. Branch on `type`, never on `msg` - "Client code should not parse and rely on the msg field." Other documented 422 types include `no_media_generated`, `image_load_error`, `image_too_large` | https://fal.ai/docs/documentation/model-apis/errors
- 2026-09-19 | A4.4a NOT CONFIRMED: neither Stripe nor Playwright documents selectors or a supported recipe for driving the hosted Checkout page from browser automation. What IS documented is that `ui_mode: hosted_page` is a full cross-origin navigation to checkout.stripe.com and NOT an iframe, and the test card details (4242 4242 4242 4242, any future date such as 12/34, any three-digit CVC). Any selector chosen on that page is unversioned Stripe UI and can break without notice | https://docs.stripe.com/testing.md
- 2026-09-19 | F1a `turnstile.reset(widgetId)` takes the WIDGET ID, not a container selector: "To reset the widget if the given widget timed out or expired, you can use the function: `turnstile.reset(widgetId);`". `turnstile.render(container, options)` RETURNS that id and it must be retained. Documented lifecycle: `const widgetId = turnstile.render("#container", {sitekey, callback}); turnstile.getResponse(widgetId); turnstile.isExpired(widgetId); turnstile.reset(widgetId); turnstile.remove(widgetId);`. `remove()` differs: "This will not call any callback and will remove all related DOM elements." | https://developers.cloudflare.com/turnstile/get-started/client-side-rendering/
- 2026-09-19 | F1b A reset regenerates the token: "the Turnstile widget needs to be refreshed to generate a new token. This can be done using the `turnstile.reset` function." NOT CONFIRMED, and this is the dangerous gap: no Cloudflare page says whether the fresh token arrives through the SAME `callback` given to `render()`, or whether anything must be re-registered. Four pages were read in full and none states it. There is no documented API for re-registering a callback. Unit F1 therefore does NOT assume the callback re-fires - it also polls `turnstile.getResponse(widgetId)`, bounded, so a fresh token is picked up whichever way it arrives. Assuming undocumented Turnstile behaviour is what caused this repository's earlier widget outage (M2) | https://developers.cloudflare.com/turnstile/get-started/server-side-validation/
- 2026-09-19 | F1c Turnstile tokens: "Tokens are single-use and expire after 300 seconds (five minutes)." Client-side expiry can be read with `turnstile.isExpired(widgetId)` and the current token with `turnstile.getResponse(widgetId)`. Every render() callback: `callback` (challenge completed, receives the token), `error-callback`, `expired-callback` (token expires before timeout), `timeout-callback` (interactive challenge timed out), `before-interactive-callback`, `after-interactive-callback`, `unsupported-callback`. `refresh-expired` controls auto-refresh: `auto` (default), `manual`, `never` | https://developers.cloudflare.com/turnstile/get-started/client-side-rendering/widget-configurations/
- 2026-09-19 | Gd gtag reads ids with `gtag('get', '<target>', '<field_name>', callback)` where target is the measurement id and the callback "will be invoked with the requested field, or `undefined` if it is unset". Supported fields include `client_id`, `session_id`, `session_number`, `gclid`. With `analytics_storage='denied'` GA "Will not read or write first-party analytics cookies" and sends cookieless pings. NOT CONFIRMED: what `gtag('get', ..., 'client_id')` actually yields while consent is DENIED - a value, `undefined`, or a callback that never fires. No Google page states it, so unit G1 must MEASURE it in a browser before any checkout call is made to depend on it | https://developers.google.com/tag-platform/gtagjs/reference
- 2026-09-19 | Ge GA4 dedup: "Google Analytics deduplicates purchase events with the same transaction ID" and "Transaction ID deduplication only works for data collected through web streams, not app streams." NOT CONFIRMED for our actual topology: no Google page says a BROWSER purchase and a MEASUREMENT PROTOCOL purchase sharing one transaction_id are deduplicated, and for the sibling event Google says the opposite - "Google Analytics doesn't automatically detect or remove duplicate `in_app_purchase` events." So the webhook stays the single sender of `purchase`; sending both would be an untested bet on the revenue number | https://support.google.com/analytics/answer/12313109
- 2026-09-19 | Gf Measurement Protocol EU host is documented - "If you want your data to be collected in the EU, change the `HOST` in the example to `region1.google-analytics.com`" - so our `https://region1.google-analytics.com/mp/collect` is right. DOCUMENTATION CONFLICT on the debug path: the endpoint table says `/_debug_/mp/collect` while the JavaScript sample on the SAME page uses `/debug/mp/collect`. NOT CONFIRMED: any regional debug host; the validation page only ever shows `www.google-analytics.com`. Settle the path with a live request, not with this line | https://developers.google.com/analytics/devguides/collection/protocol/ga4/validating-events
- 2026-09-19 | Gg `begin_checkout` parameters: `items` (Array<Item>) REQUIRED; `currency` (string) required if `value` is set; `value` (number) required for meaningful reporting; `coupon` optional. Each item needs one of `item_id` or `item_name`. Example: `gtag("event", "begin_checkout", {currency: "USD", value: 30.03, items: [{item_id: "SKU_12345", item_name: "...", price: 10.01, quantity: 3}]})` | https://developers.google.com/analytics/devguides/collection/ga4/reference/events#begin_checkout
- 2026-09-19 | Gh Google's own page: "Starting June 15, 2026, offline conversions import and enhanced conversions for leads uploads will be migrated to the Data Manager API and blocked in the Google Ads API." NOTE THE SCOPE - it names the Google Ads API as what gets blocked, NOT the file-upload UI, which is still documented (on a page now titled "legacy") | https://support.google.com/google-ads/answer/2998031
- 2026-09-19 | Gh legacy file-upload columns, still documented: `Conversion Name`, `Google Click ID` ("Don't remove the column that begins with 'Google Click ID', or your import will fail"), `GBRAID`, `Conversion Time` (format `MM/dd/yyyy HH:mm:ss` or `yyyy-MM-dd HH:mm:ss+z`, must include the time zone), `Conversion Value` (optional), `Conversion Currency` (optional, 3-letter ISO 4217). NOT CONFIRMED: a `WBRAID` column - the help page names only Google Click ID and GBRAID | https://support.google.com/google-ads/answer/7014069
- 2026-09-19 | Gh Data Manager API replacement schema, `POST https://datamanager.googleapis.com/v1/events:ingest`: click ids live under `adIdentifiers` as `gclid`, `gbraid`, `wbraid`; value is `conversionValue`; currency is `currency`; time is `eventTimestamp`, required, "RFC 3339, where generated output will always be Z-normalized"; target is `destinationReferences[]` | https://developers.google.com/data-manager/api/reference/rest/v1/events/ingest
- 2026-09-20 | MP-1 GA4 Measurement Protocol `consent` EXISTS and is OPTIONAL on a server-sent event. Payload table row: `consent` | `object` | "Optional. Consent settings for the request." Section prose, verbatim: "The `consent` attribute configures consent types and states. If you don't specify `consent`, Google Analytics uses the consent settings from corresponding online interactions for the client or app instance." Exactly TWO fields, both `string`, both Optional: `ad_user_data` - "Consent for sending user data from the request's events and user properties to Google for advertising purposes. Either `GRANTED` or `DENIED`."; `ad_personalization` - "Consent for personalized advertising for the user. Either `GRANTED` or `DENIED`." There is NO `analytics_storage` or `ad_storage` in the MP consent object - those are gtag/Consent Mode names and do not belong in this body | https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference?client_type=gtag#payload_consent
- 2026-09-20 | MP-1b NOT CONFIRMED, and this is the gap to watch: Google publishes NO JSON example of the `consent` object inside a Measurement Protocol body. The only code blocks on the reference page are for `user_location` and `device`. That `consent` is a TOP-LEVEL key (sibling of `client_id` and `events`, not nested per-event) is read off the payload table's position, not off an example. Also NOT stated anywhere: what "uses the consent settings from corresponding online interactions" resolves to when there were no online interactions for that client_id | https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference?client_type=gtag
- 2026-09-20 | MP-2 Body shape CONFIRMED. `client_id` is TOP LEVEL and required, verbatim: "You must provide `client_id` in the request body to associate the events with a particular web client instance" and "`client_id` should match the ID generated by the Google Analytics tag on your website". `session_id` is an EVENT PARAM, inside `events[].params`, per Google's own body example: `{"client_id": "CLIENT_ID", "events": [{"name": "login", "params": {"method": "Google", "session_id": "SESSION_ID", "engagement_time_msec": 100}}]}`. Also verbatim: "While `session_start` is a reserved event name, creating a new `session_id` creates a new session without the need to send `session_start`" | https://developers.google.com/analytics/devguides/collection/protocol/ga4/sending-events?client_type=gtag
- 2026-09-20 | MP-2b Joining a server event to a browser session IS documented as a requirement, per use case, verbatim: "Include the `session_id` in the event's parameter list. Send the request no later than 24 hours after the start of the online session" (session attribution), and for User-ID: "Include the `session_id` in the event's parameter list. Send the Measurement Protocol events on the _same business day_ as the online session". NOT CONFIRMED: the TYPE of `session_id`. No type column for it exists on the reference, events reference, sending-events, use-cases or troubleshooting pages; the only evidence is that every Google example writes it as a JSON STRING (`"session_id": "SESSION_ID"`). Send a string. Also NOT DOCUMENTED on any of those five pages: how to obtain the session_id from the page - the gtag `get` route is documented only on the tag-platform side (see Gd / MP-5) | https://developers.google.com/analytics/devguides/collection/protocol/ga4/use-cases
- 2026-09-20 | MP-3 CORRECTION AND WARNING for line Gf above: the debug path conflict is STILL PRESENT on the validating-events page today, in three places that disagree. The endpoint TABLE says `/_debug_/mp/collect`; the PROSE says "The validation server's URL includes "/debug" unlike the standard Measurement Protocol URL."; and BOTH JavaScript samples call `https://www.google-analytics.com/debug/mp/collect?measurement_id=${measurementId}&api_secret=${apiSecret}`. Two of the three say `/debug/`, and line 47 of this file records a live curl where `/debug/mp/collect` really did return `validationMessages`. Use `/debug/mp/collect`; do not trust the table. EU: "change the URL passed to the `fetch` method to begin with `https://region1.google-analytics.com` instead of `https://www.google-analytics.com`" - so a regional debug host IS sanctioned by that sentence, contrary to the earlier note | https://developers.google.com/analytics/devguides/collection/protocol/ga4/validating-events?client_type=gtag
- 2026-09-20 | MP-3b Validation endpoint takes the SAME body, verbatim: "The only difference in the request for events sent to the Measurement Protocol and the Measurement Protocol validation server is the URL." Query params are the same `measurement_id` + `api_secret`. Response shape, verbatim example: `{"validationMessages": [{"fieldPath": "events", "description": "Event at index: [0] has invalid name [_badEventName]. Names must start with an alphabetic character.", "validationCode": "NAME_INVALID"}]}` - so the three keys per message are `fieldPath`, `description`, `validationCode`, and a clean payload gives an empty `validationMessages` array. The same page lists the other codes (VALUE_INVALID, VALUE_REQUIRED, NAME_RESERVED, VALUE_OUT_OF_BOUNDS, EXCEEDED_MAX_ENTITIES, NAME_DUPLICATED); only NAME_INVALID is quoted verbatim above | https://developers.google.com/analytics/devguides/collection/protocol/ga4/validating-events?client_type=gtag
- 2026-09-20 | MP-4 `transaction_id` on `purchase` is REQUIRED. Row verbatim: `transaction_id` | `string` | Required **Yes** | example `T_12345` | "The unique identifier of a transaction. The `transaction_id` parameter helps you avoid getting duplicate events for a purchase." NOT CONFIRMED, deliberately: Google says "unique identifier of a transaction" and NOTHING about the scope of that uniqueness (per property? per day? forever?), nothing about what happens when one is reused, and nothing about MP-vs-gtag dedup (see line Ge). Our Stripe `session.id` is globally unique, which satisfies the strongest reading | https://developers.google.com/analytics/devguides/collection/ga4/reference/events?client_type=gtag#purchase
- 2026-09-20 | MP-5 Re-confirmed today, unchanged: `gtag('get', '<target>', '<field_name>', callback)`. `<target>` string "The target to fetch values from." (e.g. G-XXXXXXXXXX); `<field_name>` "The name of the field to get."; `callback` Function "A function that will be invoked with the requested field, or `undefined` if it is unset." Readable GA4 fields: `client_id`, `session_id`, `session_number` (and `gclid` for Google Ads / Floodlight). "Custom values set using the `set` command take precedence over default field names" | https://developers.google.com/tag-platform/gtagjs/reference
- 2026-09-20 | MP-5b STILL NOT CONFIRMED after a third check (this re-tests lines 152 and 189, do not re-research without new evidence): neither the gtag reference nor the consent devguide says one word about what `gtag('get', ..., 'client_id')` or `'session_id'` return while `analytics_storage` is DENIED. The consent page only offers URL passthrough to send "event and session-based analytics (including key events) without cookies across pages"; it never states whether a client_id exists in that state. Treat both as OPTIONAL, omit them when the callback yields `undefined`, and never block a checkout on one arriving | https://developers.google.com/tag-platform/devguides/consent
- 2026-09-20 | MP-6 NOT CONFIRMED, checked specifically and found absent: no recommendation, best-practice statement, or warning telling a developer to include `consent` on a Measurement Protocol event. Checked and silent on this: the MP reference `#payload_consent` section itself (only the already-recorded fallback sentence exists, no surrounding note/caveat), the MP policy page, the MP use-cases page (four documented use cases: User-ID, session attribution, advertising-platform export, audience creation — none is "consent"), the GA4 "Consent signal" help page (support.google.com/analytics/answer/12335634, defines Granted/Denied only), the "Verify and update consent settings" help page (browser/CMP guidance only), and the Tag Manager "Consent Type" reference (support.google.com/tagmanager/answer/13802165, client-side gtag/GTM only, explicitly does not address server-side tagging or the Measurement Protocol) | https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference?client_type=gtag#payload_consent (primary) + https://developers.google.com/analytics/devguides/collection/protocol/ga4/policy + https://developers.google.com/analytics/devguides/collection/protocol/ga4/use-cases + https://support.google.com/analytics/answer/12335634 + https://support.google.com/analytics/answer/14275483 + https://support.google.com/tagmanager/answer/13802165
- 2026-09-20 | MP-6b Server-side Google Tag Manager (sGTM) DOES have documented automatic consent-aware behaviour, but this is a DIFFERENT PRODUCT from a direct Measurement Protocol HTTP call and its guidance does not transfer. Verbatim scope statement: "This document is for developers who already have a consent solution implemented on their website and want to use consent mode in a server-side environment," and the mechanism is "The Google tag sends the user's preferences to the server container by adding consent parameters to the HTTP request" — i.e. consent flows web-container -> server-container inside sGTM's own architecture, which we do not run; our webhook posts straight to google-analytics.com/mp/collect with no sGTM in between | https://developers.google.com/tag-platform/tag-manager/server-side/consent-mode
- 2026-09-20 | MP-6c The only EEA/consent "must" language found anywhere near this topic is a GENERAL Google Analytics policy obligation, not an instruction about the MP JSON payload: Google's Measurement Protocol policy page states "You must give your end users proper notice about the implementations and features of Google Analytics that you use... You must obtain consent from your end users, or otherwise provide them with the opportunity to opt-out." This is a legal/CMP obligation on the site (get consent from the visitor in the browser), separate from and not evidence for populating the `consent: {ad_user_data, ad_personalization}` object in a server-sent event | https://developers.google.com/analytics/devguides/collection/protocol/ga4/policy
- 2026-09-20 | MP-6d ANSWER derived from MP-1, MP-6, MP-6b, MP-6c together (no single Google sentence states this conclusion, so it is an inference from what is and is not documented, not a quote): Google documents omitting `consent` and relying on the fallback ("uses the consent settings from corresponding online interactions for the client... instance") as the DEFAULT, normal-path behaviour of the field — it is not a fallback of last resort, it is simply what happens when you don't pass the object. No page anywhere (MP reference, MP policy, MP use-cases, GA4 consent-signal help, consent-settings help, Tag Manager consent-type reference, or the sGTM consent-mode doc, which is a different product) tells a developer to build and send an explicit `consent` object on a server-sent MP purchase event. Given that, sending client_id matching the real browser client_id and omitting `consent` is the documented default path, not an unsupported shortcut | synthesis of the six sources cited in MP-6/MP-6b/MP-6c above, no new URL
- 2026-09-20 | MP-7 CORRECTION to line 47's method, found while validating task 03-sale-to-visit's actual purchase payload against /debug/mp/collect: the debug endpoint does NOT enforce a named event's documented "Required" params. A `purchase` payload with `transaction_id` deleted entirely (only value/currency/items left) returned `{"validationMessages": []}` — clean — despite MP-4 stating `transaction_id` is Required: Yes. The validator DOES still catch structural defects: the same run's `{"name": "purchase!!"}` control correctly returned NAME_INVALID, so it is live and checking, just not against each named event's own required-field list. Practical consequence for this codebase: passing /debug/mp/collect proves the payload is a well-formed MP request, NOT that every field GA4's docs call required is present — tests/test_ga4.py, not the debug endpoint, is what pins transaction_id/value/currency/items being sent | live POST, both directions, https://www.google-analytics.com/debug/mp/collect?measurement_id=G-NLP25TBTRJ&api_secret=placeholder
- 2026-09-20 | MP-8 The exact payload `Ga4Purchase.__call__` builds for a delivered order carrying a PaymentIntent, a browser-reported client_id and a browser-reported session_id — `{"client_id":"1122334455.6677889900","events":[{"name":"purchase","params":{"transaction_id":"pi_3S_realpaymentintent","value":19.99,"currency":"EUR","items":[{"item_id":"corporativo","price":19.99,"quantity":1}],"session_id":"1758300000"}}]}` — was POSTed as-is (via the real `app.adapters.ga4.Ga4Purchase`, only `post` faked to capture the body) to /debug/mp/collect and returned `{"validationMessages": []}` | live POST, same endpoint as MP-7, this task
- 2026-09-21 | N1 Next.js file-based icon metadata (task 09-favicon), version 16.3.5 (our `frontend/package.json` pin) matches the fetched page. Table verbatim: `favicon` supports only `.ico`, valid location only the top-level `app/` (root segment); `icon` supports `.ico`, `.jpg`, `.jpeg`, `.png`, `.svg`, valid anywhere under `app/**/*`; `apple-icon` supports `.jpg`, `.jpeg`, `.png` only — **no `.svg`**, also `app/**/*`. Next.js auto-adds the `<link rel="icon">` / `<link rel="apple-touch-icon">` tags; no `metadata.icons` entry in `layout.tsx` is needed when the files exist by naming convention. "You can set multiple icons by adding a number suffix... Numbered files will sort lexically" and "Favicons can only be set in the root `/app` segment." | https://nextjs.org/docs/app/api-reference/file-conventions/metadata/app-icons
- 2026-09-21 | N2 Newsreader (task 09-favicon) is variable-axis only in Google's own font source repo — `google/fonts`, `ofl/newsreader/` has no `static/` subdirectory, only `Newsreader[opsz,wght].ttf` and `Newsreader-Italic[opsz,wght].ttf` (checked via the GitHub Contents API), each carrying its own `OFL.txt`. This is the same family and licence (SIL OFL 1.1, Google Fonts) already declared in `docs/DESIGN.md` for the live page's `next/font/google` load; `scripts/make_favicon.py` downloads the roman variable file directly from this repo (raw.githubusercontent.com, the canonical upstream Google publishes its fonts from) because no static instance or committed font asset exists locally to render with Pillow, and caches it under `.fonts-cache/` (gitignored) so only the first generation run needs network. Confirmed with Pillow 12.3.0 (`ImageFont.truetype(...).get_variation_axes()`): two axes, `Weight` (200-800, default 400) and `Optical Size` (6-72, default 18); `set_variation_by_axes([weight, opsz])` sets them in that order | https://github.com/google/fonts/tree/main/ofl/newsreader (file: https://raw.githubusercontent.com/google/fonts/main/ofl/newsreader/Newsreader%5Bopsz%2Cwght%5D.ttf)
- 2026-09-21 | 13a (task 13-secure-forwarding) uvicorn's own docs, verbatim: `--proxy-headers / --no-proxy-headers` - "Enable/Disable X-Forwarded-Proto, X-Forwarded-For to populate remote address info. Defaults to enabled, but is restricted to only trusting connecting IPs in the `forwarded-allow-ips` configuration." `--forwarded-allow-ips` - "Comma separated list of IP Addresses, IP Networks, or literals (e.g. UNIX Socket path) to trust with proxy headers. Defaults to `$FORWARDED_ALLOW_IPS` if set. Otherwise, `127.0.0.1` and `::1` are trusted. The literal `'*'` means trust everything." This matches what was read directly out of the installed proxy_headers.py source. www.uvicorn.org itself could not be resolved from this machine (DNS failure), so the citation is the vendor's own docs/settings.md in its source repository, the same file the published site is built from | Context7 /kludex/uvicorn + https://raw.githubusercontent.com/Kludex/uvicorn/master/docs/settings.md
- 2026-09-21 | 13b (task 13-secure-forwarding) Google's own Cloud Run docs, TWO separate pages, both verbatim. Container runtime contract, "Transport layer encryption (TLS)": "TLS is terminated by Cloud Run for HTTPS and gRPC, and then requests are proxied as HTTP/1 or gRPC to the container without TLS." Invoking with an HTTPS request, "HTTP to HTTPS redirect": "Cloud Run redirects all HTTP requests to HTTPS but terminates TLS before they reach your web service. If your service generates a web resources that refers to other web resources with unsecured URLs (`http://`), your page might be subject to mixed content warnings or errors. Use the `https` protocol for all reference web URIs or account for proxy directives in the HTTP Request such as the `X-Forwarded-Proto` HTTP header." Together these confirm the scheme uvicorn sees is http and that Cloud Run itself names `X-Forwarded-Proto` as the header carrying the real one | https://docs.cloud.google.com/run/docs/container-contract + https://docs.cloud.google.com/run/docs/triggering/https-request
- 2026-09-21 | 13c NOT CONFIRMED, checked and found absent (task 13-secure-forwarding): no sentence on any Cloud Run docs page read today (container-contract, triggering/https-request, securing/security, run/docs/issues) states that Cloud Run sets `X-Forwarded-For`, and none recommends trusting all inbound connections (`--forwarded-allow-ips=*`) on the grounds that the container is reachable only through Cloud Run's own ingress. securing/security's "Ingress (inbound) traffic" section says only that `run.app` traffic passes through GFE -> HTTP proxy -> app server before the sandbox, which is evidence the container is not reachable except through that path, but it is not the same as an explicit statement authorising `--forwarded-allow-ips=*`. Do not cite Cloud Functions' "Request Headers" page for this fact - it documents a different product | checked, absent: https://docs.cloud.google.com/run/docs/container-contract , https://docs.cloud.google.com/run/docs/triggering/https-request , https://docs.cloud.google.com/run/docs/securing/security , https://docs.cloud.google.com/run/docs/issues
- 2026-09-21 | task 21-price-mode-visible: stripe-python 15.6.1's `RequestOptions` TypedDict (the kwargs accepted alongside `**params` on legacy resource classmethods like `Price.retrieve`) has NO `timeout` field — Context7 (/stripe/stripe-python) query-docs for "timeout" returned only `max_network_retries` (retry count, not a deadline) and custom-HTTP-client sections, never a per-call timeout kwarg. The documented, working mechanism is `stripe.default_http_client = stripe.RequestsClient(timeout=<seconds>)` (its default is 80s), confirmed by reading the installed package's own source: `inspect.signature(stripe.RequestsClient.__init__)` -> `timeout: Union[float, Tuple[float, float]] = 80`. `_price_is_live` in app/entry.py sets this before the one Price.retrieve call at startup, bounding it to 5s so a hung Stripe API delays a cold start rather than hanging it | Context7 /stripe/stripe-python + installed stripe-python 15.6.1 source (stripe/_http_client.py)
