# Design critique — real frontend, round 3 (final) — 2026-09-17 22:00

Served the freshly rebuilt `frontend/out` via `python -m http.server 8903` (killed at the
end of this review — confirmed down: `curl` to `localhost:8903` after kill returns no
response). Captured with Playwright at 390x844 and 1440x900. No backend on localhost;
`/api/preview` and `/api/checkout` are unreachable by construction, so the preview
loading/rendered states are still not capturable — reported honestly below, not invented.

Screenshots beside this file: `landing-390x844.png`, `landing-1440x900.png`,
`upload-hover-390x844.png`, `upload-focus-390x844.png` (Tab now lands on the file input
correctly, confirmed fresh-navigate — a stray earlier reading in my own script, caused by
a manual `blur()`+re-hover sequence before the check, was discarded and re-verified),
`upload-filechosen-390x844.png`, `cta-focus-390x844.png`, `gallery-390x844.png`,
`gallery-1440x900.png`, `recuperar-390x844.png`, `recuperar-1440x900.png`,
`recuperar-invalidemail-390x844.png`, `legal-terminos-390x844.png`,
`legal-terminos-1440x900.png`.

`python scripts/design_audit.py frontend/out` → `DESIGN AUDIT: 0 P0, 0 P1, 0 P2` — `PASS
(floor only)`, same clean floor as round 2; the audit still cannot see any of the four
fixes or the two open issues below, all of which were found by looking.

---

| # | Line | Score | Evidence |
|---|---|---|---|
| 1 | Decided or averaged | 4 | The fraunces-fallback regression from round 2 is gone: `getComputedStyle` on the live H1 of `/recuperar/`, `/g/?o=...`, and `/legal/terminos/` all return `fontFamily: "Newsreader, \"Newsreader Fallback\""` — the real webfont, not a silent fallback to Public Sans — matching `frontend/src/app/recuperar/page.tsx:54`, `frontend/src/app/g/page.tsx:79`, `frontend/src/components/legal-page.tsx:21`, all now `var(--font-newsreader)`. Every one of the seven routes now carries the same type pairing, palette and motion, not just the homepage. Docked one point, not five: `frontend/src/app/page.tsx:67`'s hero still has an unaddressed height mismatch (the "Empieza aquí" card's bounding box ends at `bottom: 253.5px` at 1440px width, `getBoundingClientRect` confirmed, while the left column runs another ~300px past it before the divider — visible as dead white space in `landing-1440x900.png`), and the primary CTA (`components/ui/button.tsx:29`, size `lg` → `h-9` = 36px) is 8px short of the 44px tap-target floor on the one button the entire funnel depends on — a decided page checks its own numbers against its own rule. |
| 2 | Typographic hierarchy | 4 | Confirmed by direct query, not just class names: `/recuperar/` H1 is Newsreader at `30px`/weight `400`, its body `<p>` is `"Public Sans"` at `16px`/`400` — a real family-plus-size step, not the round-2 fallback where both collapsed to the same font. Same check on `/g/` (H1 `36px` Newsreader) and `/legal/terminos/` (H1 `30px` Newsreader). Homepage keeps its three-step scale (H1 `clamp(38px,6.4vw,68px)` Newsreader / 16px Public Sans body / 11px Chivo Mono labels, `page.tsx:70,86`), and the H1 itself now mixes weights within one line — "Tu **foto de perfil profesional**, revelada en dos minutos" — bold serif for the ad-copy phrase against light-weight serif for the rest, underlined in the accent red (`landing-1440x900.png`), which is a genuine typographic decision, not just a size bump. Held at 4, not 5, because none of the H1s carry weight contrast against body beyond size and colour — all render at weight 400. |
| 3 | Colour | 5 | Unchanged and unbroken by the four fixes: `getComputedStyle(document.documentElement)` returns `--background:#f2f1ed`, `--foreground:#141312` (6.25:1 body-on-paper), `--primary:#c3352b` (used only as the CTA fill, the hover/focus border, and mono labels — 4.8:1 on paper), `--radius:0px`. Confirmed identical on landing, gallery, recuperar and legal captures — no drift, no third hue introduced by the new hover/focus states (they reuse the same accent and a `#e6e4de`-ish secondary, not a new colour). |
| 4 | Layout | 4 | `sm:grid-cols-[2fr_1fr_1fr]` (`page.tsx:107`) still ships the real 2:1:1 "Cómo funciona" grid, confirmed visually unchanged from round 2 in `landing-1440x900.png`. Same one-point deduction as round 2 for the same reason: the hero's right-hand card (`items-start` on an unequal-height two-column grid, `page.tsx:67`) leaves an unbalanced gap below it at 1440px — this was not one of the four things fixed this round, and it is still visible. |
| 5 | Value above the fold | 2 | Unchanged, correctly so: the four "Muestras" frames still read "Aquí aparecerá un ejemplo real en cuanto tengamos fotos aprobadas para publicar" (`page.tsx:149`-adjacent, confirmed in `landing-390x844.png` and `landing-1440x900.png`) — an honest placeholder, no faked result anywhere in any of the twelve captures. Scored at the rubric's explicit placeholder ceiling. **With real before/after photographs (issue #6) this would plausibly score 4–5** — the rest of the page is image-led in intent (dashed frame, numbered "01–04" labels, a real photo well inside the fold at 1440px) and is waiting on nothing but the asset. |
| 6 | Trust for a Spanish buyer | 4 | Improved from round 2's 3: the one concrete trust defect that was fixable this round — the native, untranslated English validation bubble on `/recuperar/` — is gone. Submitting `notanemail` now renders our own copy, `role="alert"`, in Spanish: "Escribe un correo válido, por ejemplo nombre@dominio.com." (`recuperar/page.tsx:29`, confirmed live in `recuperar-invalidemail-390x844.png`), styled in the site's own red, not a browser tooltip. IVA incluido inline with price (home and `/legal/terminos/`), the one-condition refund rule stated twice verbatim, 7-day/1-year deletion timing, AI disclosure in every footer, and `/recuperar/` linked from every footer — all still present and unchanged. Held at 4, not 5, because `/legal/terminos/` still renders a live `[PENDIENTE: nombre o razón social]` bracket in the Contacto section (`legal-page.tsx:33`, confirmed in `legal-terminos-390x844.png`) — a Spanish buyer checking who they are paying still finds a visible placeholder. This is issue #3, explicitly out of scope for this round (inventing a legal entity name is not Kevin's to delegate to an agent) — **with it filled in, this line would be 5**. |
| 7 | Thumb reach / tap targets | 3 | The round-2 blocker here — the raw 20px-tall OS file input as the funnel's first tap target — is fixed: the visible tap target is now the `<label>`, measured by `getBoundingClientRect` at `292×172px`, an order of magnitude over the 44px floor, and it sits well within the thumb arc (`top: 557.75` of an 844px viewport). But a new, more consequential miss surfaces on direct measurement: the primary CTA button — the one the buyer taps to see the free preview and, later, to pay — is `size="lg"` in `components/ui/button.tsx:29` (`h-9` = `36px`), measured live at `w:292 × h:36`. That is 8px under the 44px floor, on the single tap target the entire commercial funnel runs through. Not as severe a miss as round 2's 20px control, but it is the more important element to get right. |
| 8 | States | 4 | Every state the brief asked for that is reachable without a backend now has a deliberate, verified treatment. **Hover**: `getComputedStyle` on the drop-zone label moves from `border:#141312`/`background:transparent` at rest to `border:#c3352b`/`background:rgb(230,228,222)` on hover — a real, visible change (`upload-hover-390x844.png`). **Keyboard focus** on the (now sr-only, still-focusable, `tabIndex:0`) file input turns the same label's border to `#c3352b` without the background change, a distinct, real focus signal (`upload-focus-390x844.png`). **File-chosen**: the raw OS control is gone from view; the label now reads "1 de 4 elegidas / Elegir otras fotos / landing-390x844.png" — the chosen filename is reported back inside the hand-typeset frame (`upload-filechosen-390x844.png`). **CTA keyboard focus**: `getComputedStyle` on the focused button returns `animationName:"sf-draw"`, `animationDuration:"0.22s"`, `boxShadow: "#f2f1ed 0 0 0 2px, #c3352b 0 0 0 5px"` — the bespoke ring the round-2 review found wired only to footer links now fires on the actual checkout button (`cta-focus-390x844.png`). **Invalid-email error**: our own Spanish copy, not a browser bubble (see line 6). Held at 4, not 5, because the busy/loading state (`Progress` bar + "Generando tu prueba…" copy, `upload-form.tsx:139,163`) and the true preview-rendering/rendered states remain unverified this round — the code is unchanged since round 2's read of it, but with no backend on the static export I could not click through and watch it happen live, so I am not scoring code I read as code I saw. |
| 9 | Motion | 5 | Unchanged: `.sf-focus:focus-visible{animation:.22s ease-out sf-draw}` (now visibly reaching the CTA, see line 8) and `.sf-frame:hover{transform:translateY(-3px);transition:transform .14s}` are the only two motion rules in the stylesheet, both wrapped by the same `prefers-reduced-motion:reduce` guard (`globals.css:228-231`), confirmed by reading the shipped CSS. Nothing decorative anywhere. |
| 10 | Slop tells | 4 | `design_audit.py` stays clean (0/0/0) on all three counts, as it did in round 2 — it cannot see typography fallback, placeholder brackets, or sub-floor tap targets. The round-2 tell — a page that looks decided in one place (home) and silently reverts to the pre-decision default two clicks away (recuperar/gallery/legal) — is gone, verified live across all three secondary routes. Two smaller tells remain, both visual, both honestly disclosed rather than hidden: the live `[PENDIENTE: …]` bracket on `/legal/terminos/` (still reads as "shipped before finished" to a buyer who does not know it is intentionally deferred), and the 36px CTA falling 8px short of a well-known, easily-checked accessibility number — the kind of thing that suggests the button's height was inherited from a default size token rather than checked against the 44px rule directly. |

**Total: 39/50.**

No line scores 0 or 1 — no automatic blocker under the rubric's own rule. Total clears
the 35 floor (PASS by the rubric's stated threshold). Line 5 sits at 2, below the "no
line under 3" bar the brief set as this round's target — see verdict.md for why that
specific target is not reachable right now regardless of how the other nine lines score.
