# Design critique — real frontend, round 2 — 2026-09-17 21:15

Served `frontend/out` (already built, not rebuilt) via `python -m http.server 8902`.
Captured with Playwright at 390x844 and 1440x900. No backend on localhost; states that
require it are reported as what actually happens (a fetch failure), not invented.

Screenshots beside this file: `landing-390x844.png`, `landing-1440x900.png`,
`upload-hover-390x844.png`, `upload-focus-390x844.png` (Tab lands on the native file
input, not the CTA), `upload-filechosen-390x844.png`, `cta-focus-390x844.png` (Tab
again, to the now-enabled CTA), `gallery-1440x900.png`, `gallery-390x844.png`,
`recuperar-1440x900.png`, `recuperar-invalidemail-390x844.png`,
`legal-terminos-390x844.png`. `scripts/design_audit.py frontend/out` → `0 P0, 0 P1, 0
P2, PASS (floor only)`.

`--radius:0px`, no `box-shadow` on any card/frame/button (`getComputedStyle` on the
hero card and CTA both return `boxShadow: none`), and the wordmark is plain
`Studioface` (footer, no split colour) — all three confirmed by direct DOM query.

---

| # | Line | Score | Evidence |
|---|---|---|---|
| 1 | Decided or averaged | 3 | The homepage is a real decision: `frontend/src/app/page.tsx:5-20` documents in comments exactly which prior-critique findings it fixes, the card is gone (a ruled `border` frame, not `shadow-lg`), and no shadow/radius survives anywhere. But three of the site's seven routes (`/recuperar/`, `/g/`, all four `/legal/*`) still reference `font-[family-name:var(--font-fraunces)]` (`frontend/src/app/recuperar/page.tsx:45`, `frontend/src/app/g/page.tsx:79`, `frontend/src/components/legal-page.tsx:21`) — Fraunces was the exact serif DESIGN.md killed as half of the "tasteful-default" P0 (docs/DESIGN.md:16), and `--font-fraunces` is defined nowhere in the shipped CSS, so those H1s silently fall back to plain Public Sans at 400 weight (confirmed via `getComputedStyle`). Most of the site was never migrated to direction B; it was left on the old, rejected default. |
| 2 | Typographic hierarchy | 3 | Homepage: Newsreader `clamp(38px,6.4vw,68px)`/1.02 H1 against 16px Public Sans body and 11px letterspaced Chivo Mono labels (`frontend/src/app/page.tsx:70,86`) is a genuine three-step scale; lede is `max-w-[52ch]` and FAQ answers `max-w-[60ch]` (page.tsx:77,126), both inside the 45–75ch band (confirmed in landing-1440x900.png). But on `/recuperar/`, `/g/`, and `/legal/terminos/` the H1 renders at 36px/**weight 400** in the same Public Sans as the body (legal-terminos-390x844.png, gallery-390x844.png) — the fallback from finding 1 collapses the family contrast to a bare size bump, so those pages have a two-step scale where the flagship page has three. |
| 3 | Colour | 5 | `--background:#f2f1ed` / `--foreground:#141312` (computed contrast 6.25:1 body-on-paper, well past 4.5:1) / `--primary:#c3352b` (4.8:1 on paper, used only as CTA fill, underline, and label text, never as body copy) — confirmed by direct `getComputedStyle` query and visible identically across landing, gallery, recuperar and legal captures. One dominant, one accent, no third hue, no drift even on the broken-font pages. |
| 4 | Layout | 4 | `frontend/src/app/page.tsx:107` ships `sm:grid-cols-[2fr_1fr_1fr]` for "Cómo funciona" — confirmed visually in landing-1440x900.png: column one runs ~555px, columns two and three ~275px each, a real 2:1:1 read, not three equal blocks. The 7fr/5fr hero (page.tsx:67) keeps H1, price and CTA inside the fold at 1440x900 with no scroll (landing-1440x900.png), fixing A's blocker outright. Docked one point: at 1440 the right-hand upload card ends ~200px above where the left column's trust copy ends, leaving an unbalanced dead-white gap in the hero (`items-start` on an unequal-height 2-col grid, page.tsx:67) — asymmetry that reads as a height mismatch rather than a chosen composition. |
| 5 | Value above the fold | 2 | Still an honest placeholder only — "Aquí aparecerá un ejemplo real…" (page.tsx:149) — capped per rubric; no faked result anywhere in the four capture states. |
| 6 | Trust for a Spanish buyer | 3 | IVA incluido inline with price on both home and `/legal/terminos/` ("Por 19,99 €, IVA incluido…", legal-terminos-390x844.png), the one-condition refund rule stated twice verbatim, deletion timing (7 days/1 year) on both home FAQ and legal terms, AI disclosure in the footer of every page, and `/recuperar/` reachable from the footer on every page. But `/legal/terminos/` ships a live, unfilled `[PENDIENTE: nombre o razón social]` bracket in the Contacto section (legal-terminos-390x844.png) — a Spanish buyer checking who they're paying finds a visible "TODO," which is a direct hit against the identity-disclosure a legal page exists for. And the one error state the buyer will actually hit on `/recuperar/` is an **English** native browser tooltip ("Please include an '@' in the email address…", recuperar-invalidemail-390x844.png) on an otherwise 100% Spanish product. |
| 7 | Thumb reach / tap targets | 3 | The CTA (`Button size="lg"`) and upload label both sit within the first ~700px of the 844px viewport (landing-390x844.png) — reachable without a stretch. But the actual native `<input type="file">` the browser renders inside the drop-zone (`frontend/src/components/upload-form.tsx:124-134`, unstyled, `className="mt-4 block w-full text-sm"`) measures 20px tall by direct `getBoundingClientRect()` query — well under the 44px floor, and it is the literal first tap target in the funnel. |
| 8 | States | 2 | Concretely checked: **hover** on the drop-zone label produces zero visual change (upload-hover-390x844.png vs landing-390x844.png are pixel-identical in that region — no CSS hover rule exists on the label in upload-form.tsx). **File-chosen** shows the raw, unstyled OS control — "Choose Files b-1440x900.png" — sitting inside an otherwise fully custom-typeset frame (upload-filechosen-390x844.png). **Keyboard focus** on the primary CTA (the state the brief named specifically) is the generic shadcn `focus-visible:ring-3 focus-visible:ring-ring/50` box-shadow glow with `animation:none` (confirmed via `getComputedStyle`) — the bespoke `.sf-focus{animation:.22s ease-out sf-draw}` treatment exists in the CSS but is wired only to the footer `<a>` legal links, never to `components/ui/button.tsx`, so the one button that matters most never draws. **Invalid-email error** on `/recuperar/` is a pure, untranslated native browser validation bubble — no custom error UI is rendered at all. Partial credit: the `busy` state does swap CTA copy to "Generando tu prueba…" with a `Progress` bar (upload-form.tsx:139,163), and the gallery's "enlace no válido o caducado" empty/error state is a real bespoke component (gallery-390x844.png), just typeset in the fallback font from finding 1. |
| 9 | Motion | 5 | Two named moments, both confirmed in the shipped CSS and both correctly guarded: `.sf-focus:focus-visible{animation:.22s ease-out sf-draw}` and `.sf-frame:hover{transform:translateY(-3px);transition:transform .14s}`, with `@media(prefers-reduced-motion:reduce){.sf-focus:focus-visible,.sf-frame{transition:none;animation:none}}` wrapping both. Nothing decorative found anywhere in the stylesheet. |
| 10 | Slop tells | 2 | `design_audit.py` is clean (0/0/0) — it cannot see either of these. Visually: the `--font-fraunces` fallback (finding 1) is exactly the "half-migrated template" tell — a page that looks decided in one place and reverts to the pre-decision default two clicks away. The live `[PENDIENTE: …]` placeholder bracket on `/legal/terminos/` and the raw OS "Choose Files" control inside a hand-typeset frame are both the same species of tell: visible evidence the build was shipped before it was finished, which is a stronger signal against "decided" than any single hex or font choice. |

**Total: 32/50 — below the 35 floor and well short of the 40 target.**

No single line scored 0 or 1, so there is no line-level automatic blocker by that rule,
but three lines (5, 8, 10) sit at the rubric floor of 2, and the total fails outright.
