# Fingerprints gitleaks may skip, each with the reason it is not a secret.
#
# `gitleaks git` scans the ENTIRE history, not the commits being pushed, so a finding in
# a commit that is already on the remote blocks every future push until it is resolved
# here or the history is rewritten. Rewriting main to remove a published public key would
# be a far bigger action than the finding warrants.
#
# 31c5fc8 — the p= value of resend._domainkey.studioface.app, quoted in HANDOFF.md while
# writing up the DMARC work. A DKIM public key is published in DNS for every receiving
# mail server on the internet to read; that is the entire point of it. It is high-entropy
# base64, which is why the generic-api-key rule fires, and it is not a secret in any
# sense. The bytes have since been removed from both HANDOFF.md and docs/verified.md,
# which now say an RSA public key is present and leave it at that — so this entry covers
# one historical commit and nothing going forward.
#
# Anything added here needs a line saying why. An allowlist without reasons is an
# allowlist nobody can audit, and this file is the one place a real secret could be
# waved through.
31c5fc85a06dfcccee8ac3614d7aaf58f8476b92:HANDOFF.md:generic-api-key:838

# c54b2b6 — the current live Stripe webhook ENDPOINT id (starts `we_`, ends
# `DV4oNARv`; full value in docs/GO-LIVE-KEVIN.md and HANDOFF.md), read from
# `terraform state show` in the task 17 go-live audit. That is an identifier used to
# route events, not a signing secret — the signing secret is the `.secret` attribute
# on that same object, which was never read, printed, or written anywhere in this
# task. It is high-entropy, which is why the generic-api-key rule fires, same
# false-positive shape as the DKIM key above. The same class of value (Stripe object
# ids) is already recorded in the clear in docs/stripe-test-objects.md and
# docs/GO-LIVE.md. Not spelled out in full here on purpose, so this entry's own text
# does not reintroduce the same finding on itself.
c54b2b6dd79f7e4366ebf790c3ba85b6455ecbc5:docs/audit/go-live-dry-run-2026-09-20.txt:generic-api-key:125

# 0c7cbd1 — scripts/check_gallery_privacy.py's own KEY constant,
# "0123456789abcdef0123456789abcdef". The script's docstring says exactly what it is:
# a made-up gallery token opened against a made-up order id so the check "costs
# nothing and sends nothing" — never a value issued by delivery_token() for any real
# order. It is 32 hex characters, the same width delivery_token() produces, which is
# why the generic-api-key rule fires on it: same false-positive shape as the two
# entries above, a high-entropy literal that is public/synthetic by construction
# rather than a credential. The task that added this file said to keep it byte for
# byte as committed, so the fix is here, not in the script.
0c7cbd13d605c0abad17a688d24ac6b07bf95cbe:scripts/check_gallery_privacy.py:generic-api-key:18

# 3e218c0 — the task 31 HANDOFF.md write-up quoted that same synthetic KEY constant
# in full, formatted as `KEY = "..."`, which is exactly the assignment shape the
# generic-api-key rule matches (the entry directly above, written as a plain comment
# with no "=", did not trip it). A later commit on this same branch rewrote that
# paragraph to describe the constant instead of quoting it; this entry only covers
# the earlier snapshot, which still exists in history.
3e218c07b977b536748fbb40dc5649982f2bb55b:HANDOFF.md:generic-api-key:3743

# 9e64df1 (task 95b) — the IndexNow key in frontend/src/content/indexnow.json. Public by
# design: IndexNow proves site ownership by fetching this exact value from
# https://studioface.app/<key>.txt, where it is served to anyone, and scripts/
# verify_production.py checks it is. 32 hex characters, which is why generic-api-key
# fires. A secret would be worthless the moment it is served; this one only works because
# it is.
9e64df13a9cf10cb757fb0ce5ffeb118f5d17905:frontend/src/content/indexnow.json:generic-api-key:2

# 9e64df1 (task 95b) — GOOD_KEY in tests/test_indexnow.py, "0123456789abcdef" twice: a
# made-up key in the valid shape for the stubbed tests. It is not and never was anyone's
# key; no network call in that file is real.
9e64df13a9cf10cb757fb0ce5ffeb118f5d17905:tests/test_indexnow.py:generic-api-key:38
